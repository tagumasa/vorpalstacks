name: vorpalstacks
description: "VorpalStacks Admin Console"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  grpc: ^5.1.0
  protobuf: ^6.0.0
  fixnum: ^1.1.0
  flutter_secure_storage: ^10.0.0
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
