# Changelog

All notable changes to Vorpalstacks will be documented in this file.


## [0.1.0] - Upcoming

Vorpalstacks will switch to **MIT + Apache 2.0** dual licensing,
replacing the current FSL-1.1-MIT (root) / Apache 2.0 (pkg/) split.

### Added

- **ACM** — ImportCertificate re-import (CertificateArn updates existing IMPORTED cert); RequestCertificate/admin shared issuance; field validation (IdempotencyToken, Tags, PcaArn, DaysBeforeExpiry, DomainValidationOptions); error expansion (InvalidStateException, RequestInProgressException, ResourceInUseException); SearchCertificates FilterStatement structural validation with AcmCertificateMetadataFilter enum enforcement (Status, RenewalStatus, Type, InUse, Exported, ExportOption, ManagedBy, ValidationMethod, CertificateKeyPairOrigin) and CertificateArn filter validation; X509 attribute extraction; CertificateSearchResult AcmCertificateMetadata union; ImportCertificate size limits (Certificate 32 KB, PrivateKey 5 KB, Chain 2 MB); CommonName/DnsName filter @length(1-256) and ComparisonOperator required (EQUALS/CONTAINS); RSA_1024 rejected at issuance with Minimum RSA_2048 enforcement; DeleteCertificate InUseBy guard with cross-service ACMInvoker.
- **API Gateway** — COGNITO_USER_POOLS authorizer (CognitoTokenValidator, ProviderARNs); DynamoDB FilterExpression/ProjectionExpression (BETWEEN/IN); Lambda proxy cookies/multiValueHeaders, authorizer isAuthorised; non-proxy integration pipeline (templates, passthrough, binary, responseTransferMode); MethodSettings/CanarySettings patch; stage throttling (token-bucket); TlsConfig, inline IntegrationResponses, AuthorisationScopes; RestApi DisableExecuteApiEndpoint, SecurityPolicy, EndpointAccessMode, ApiStatus/ApiStatusMessage, cloneFrom, apiKeySource validation; DomainName MutualTls, RoutingMode, FQDN/securityPolicy, managementPolicy, ACM cert required, UpdateDomainName patch (endpointConfiguration/types, mutualTlsAuthentication); UpdateIntegration tlsConfig/insecureSkipVerification; DeleteRestApi base path mapping cascade; max body 413; HTTP executor 504 on deadline; input validation (stage name, HTTP method, auth type, path-part, integration type, minimumCompressionSize, API key, GetUsage 90-day, authoriser TTL 0–3600, usage plan, quota period, pagination).
- **AppSync** — PER_RESOLVER_CACHING with SHA-256 key and TTL expiry; API key expiry validation and count limit; CachingConfig TTL validation; combined API quota; tags in GraphQL API, Event API, and Channel Namespace responses; AppSyncRuntime validation; SourceApiAssociationConfig with MergeType; GraphQL data-plane multi-provider authentication (x-api-key header selects API_KEY, IAM fallback, additionalAuthenticationProviders, fail-closed for OPENID_CONNECT/AMAZON_COGNITO_USER_POOLS/AWS_LAMBDA); engine errorType propagation across resolver/template/dispatch errors for HTTP status mapping (INTERNAL_FAILURE → 500); resolver mutation schema cache invalidation; tag resource validation for resolver, function, type, API key, API cache, and domain name ARN paths; pagination maxResults range enforcement (Smithy @range 0–25); real introspection JSON from parsed SDL; includeDirectives honoured; protocol-specific access denied error codes in dispatcher; Event API data-plane auth enforcement on WebSocket/HTTP publish and subscribe (API_KEY store lookup + expiry, AWS_IAM SigV4 connection-level verification, AMAZON_COGNITO_USER_POOLS JWT via CognitoTokenValidator, AWS_LAMBDA authoriser invocation with isAuthorised, OPENID_CONNECT fail-closed); shared tag validation utility (50-tag limit, key 1–128, value 0–256, aws: prefix reserved); parent API existence validation on child resource creation (DataSource/Function/Resolver/Type → GraphQL API, ChannelNamespace → Event API, ApiKey → either); channel namespace deletion cleans up active subscriptions; merged API association secondary index (associationId → mergedApiId, O(1) reverse lookup); admin CreateGraphqlApi rejects unknown authentication types; ApiCache TTL range enforcement (1–3600).
- **Athena** — BytesScannedCutoffPerQuery enforcement (≥10000000); StartQueryExecution rejects disabled workgroups, validates ClientRequestToken [32–128], supports idempotent re-submission; FULL/RIGHT/LEFT JOIN, FULL OUTER JOIN, NATURAL FULL JOIN, UNION/UNION ALL query execution; capacity reservation APIs with status guards (cancel→ACTIVE, delete→CANCELLED); Smithy-conformant validators (WorkGroupName [1–128], StatementName [1–256], DataCatalogType/WorkGroupState enums, TargetDpus ≥4, tag key/value length, AdditionalConfiguration [1–128], ExecutionRole IAM ARN); MaxResults range validation with 0-as-default across all list operations; INSERT atomicity via AppendRows with per-table key lock; evaluateWhere BETWEEN/NOT BETWEEN; ResultConfiguration ACL/Encryption/ExpectedBucketOwner/Remove* fields; WorkGroup configuration (AdditionalConfiguration, ExecutionRole, CustomerContentEncryptionConfiguration); shared core validation across HTTP and admin handlers via workgroup_core.go with AWSErrorToGRPC.
- **CloudFront** — PublicKey and KeyGroup CRUD with name uniqueness, distribution-reference guards, and quota enforcement; If-Match ETag enforcement on Update/Delete for keys, key groups, policies, OriginAccessControl, and Distribution; distribution config round-trip expansion (associations, error responses, geo restrictions, forwarded values, trusted signers/key groups — were hardcoded to empty defaults); Invalidation persistence (PebbleDB-backed); distribution and invalidation async deployment lifecycles; duplicate-name collision detection on policies and OriginAccessControl; managed-policy type flag for listing; DeleteDistribution cleans up orphaned invalidations; ACM certificate usage tracking on distribution lifecycle with pre-validation and compensating transactions; WAF association failure cleanup on CreateDistribution/CreateDistributionWithTags.
- **CloudTrail** — CloudTrail Lake query API (StartQuery with SELECT/FROM/WHERE parser, GetQueryResults, DescribeQuery, CancelQuery, ListQueries); GenerateQuery and SearchSampleQueries; import API with async lifecycle; AdvancedEventSelectors on PutEventSelectors; channel management for external data ingestion (CRUD with Tags); EDS ingestion control, soft-delete with RestoreEventDataStore, and federation; BillingMode enum validation; event configuration; delegated admin registration; paginated ListEventDataStores; EDS name, RetentionPeriod, and tag validation; LookupEvents EventCategory filter and MaxResults cap.
- **CloudWatch** — Extended statistics (percentile, trimmed mean, winsorised mean, interquartile mean) on alarms and metric queries; metric math expression engine with period-aligned grid expansion; composite alarm evaluation with AlarmRule boolean parser and cyclic-dependency detection; anomaly detection models with ANOMALY_DETECTION_BAND; Contributor Insights rules (CRUD, managed rules, InsightRuleReport); alarm mute rules with per-region suppression; PutLogAlarm with ScheduledQueryConfiguration; metric math alarm evaluation; DescribeAlarms composite-rule traversal and filters; DescribeAlarmHistory and ListMetrics filters; PutMetricAlarm/PutCompositeAlarm validation (Statistic whitelist, bounds, Actions caps); Dashboard Tags; EvaluationCriteria with PromQLCriteria.
- **CloudWatch Logs** — LogGroup KmsKeyId, DeletionProtectionEnabled, and LogGroupClass with validation; chunk format V2 with per-entry ingestion timestamp; PutLogEvents atomic commit with orphaned chunk cleanup; resource/account/data-protection policy CRUD with optimistic locking; Logs Insights query engine with CWLI pipeline parser; scheduled queries with rate-based worker; export tasks with async gzip-to-S3; import tasks with async S3 ingestion; query definitions with queryLanguage; KMS key association; log fields APIs; TestMetricFilter extractedValues via filterpattern; MetricTransformation DefaultValueSet flag; CreateLogGroup rollback on tag failure.
- **Cognito Identity** — STS-backed credential issuance for GetCredentialsForIdentity (resolving roles from pool config with CustomRoleArn override); RS256-signed JWT OpenID tokens for GetOpenIdToken and developer identity variants; GetId identity reuse for authenticated logins with AccountId validation; LookupDeveloperIdentity pagination; GetOpenIdTokenForDeveloperIdentity TokenDuration parameter and PrincipalTags embedded as `cognito:principal_tags` JWT claim for STS session tag propagation; DeveloperUserAlreadyRegisteredException on conflicting IdentityId; exactly-one-Login enforcement; MergeDeveloperIdentities logins consolidation; ListIdentities HideDisabled parameter; Smithy validators across pool operations (IdentityPoolName, DeveloperProviderName, ProviderName, ClientId, RoleMapping Type/AmbiguousRoleResolution/MatchType, Claim 1–64, RoleARN 20–2048); UpdateIdentityPool enforces required IdentityPoolName and AllowUnauthenticatedIdentities per Smithy; admin console tag persistence with rollback on failure; admin handler delegates to transport-agnostic Core methods shared with HTTP API.
- **Cognito IDP** — Per-client token validity from UserPoolClient config; ID token persistence for revocation; USER_SRP_AUTH with Cognito SRP-6a and HKDF; Lambda trigger ClientMetadata/ValidationData propagation; device tracking with SRP; per-user MFA preferences and auth event audit; token auth lifecycle (RevokeToken, verification codes, constant-time comparison); WebAuthn credentials with custom-domain RP ID; user import jobs; managed login branding and terms CRUD; identity provider AttributeMapping and IdpIdentifiers; risk configuration; client secret lifecycle; UserPoolClient/UserPool/LambdaConfig fields per Smithy; PasswordHistorySize policy with range validation; ForceAliasCreation on AdminCreateUser; AdminResetUserPassword CustomMessage trigger; MFA configuration types; DeleteUserPool cascade; auto-verified attribute marking; store-level pagination for ListUsersInGroup and AdminListGroupsForUser; Smithy MaxResults caps; cn- partition JWKS hostname support.
- **DynamoDB** — Kinesis Data Stream destinations on item mutations; Global Tables multi-active replication; TTL expiration worker; GSI backfill on UpdateTable; auto-scaling settings round-trip; condition expression BETWEEN, IN, and NOT operators; CSV import format; ExpectedRevisionId optimistic concurrency on resource policies; WarmThroughput, OnDemandThroughput, and GlobalTableSourceArn on CreateTable; backup async lifecycle; export/import FAILED status with FailureCode/FailureMessage; ExportFormat validation; UpdateKinesisStreamingDestination precision; PartiQL if_not_exists() in SET.
- **EC2** — DryRun parameter support across all operations (HTTP 412 DryRunOperation); TagSpecification.N.Tag.N.Key/Value parsing (AWS SDK Go v2 wire format) alongside Tag.N.Key/Value (AWS CLI v1); CreateSecurityGroup response SecurityGroupArn and TagSet; CreateSubnet SubnetArn and SubnetType; CreateVpc DhcpOptionsId and IsDefault; Subnet CIDR overlap validation (InvalidSubnet.Conflict); DeleteSecurityGroup DependencyViolation when another security group references the target via UserIdGroupPairs; cross-service dependency checks on DeleteSubnet and DeleteSecurityGroup via SubnetUsageChecker and SecurityGroupUsageChecker interfaces (Lambda VpcConfig, Neptune DB subnet groups); Smithy-spec Subnet fields (AvailabilityZoneId, DefaultForAz, AssignIpv6AddressOnCreation, Ipv6Native, EnableDns64, SubnetArn, SubnetType).
- **EventBridge** — Scheduled rule evaluation (rate() and cron() with field-level validation and DOW name expansion); target retry policy with exponential backoff and DLQ routing; Kinesis PartitionKeyPath and SQS MessageGroupId targets; RunCommand, Batch, RedshiftData, SageMakerPipeline, and AppSync target parameters; archive retention worker; PutTargets quota enforcement; PutEvents honours DetailType/Source, accepts EndpointId, propagates TraceHeader; connection AuthParameters (BASIC/OAUTH/API_KEY with redacted Describe); event bus KmsKeyIdentifier, DeadLetterConfig, and LogConfig; archive KmsKeyIdentifier and StateReason; DeleteRule Force cascade; PutPermission length enforcement; TEST_MODE-aware retry defaults to prevent delivery-pipeline goroutine exhaustion during integration tests; Smithy-spec response shapes.
- **IAM** — Entity name pattern validation per Smithy types; policy document validation split into managed and trust policies with Action/NotAction and Resource/NotResource mutual exclusivity, empty-value-as-absent hasPolicyKey, and document-level Version validation; SimulatePrincipalPolicy with real evaluation (inline, managed, group, permissions boundary); password policy validation with bcrypt reuse prevention; pagination across policy, group, user, and certificate listings; tag validation; resolveUserName defaults to authenticated caller; OIDC/SAML provider validation including assertionEncryptionModeType and privateKeyIdType; IAM policy ARN validation on attach/detach (customer-managed and AWS-managed patterns); Smithy assertionEncryptionModeType, privateKeyIdType, pathType, thumbprintType (hex), clientIDType, serviceNamespaceType, customSuffixType, roleDescriptionType, roleMaxSessionDurationType, and policyScopeType validation across Create/Update/List; PermissionsBoundaryUsageCount via Increment/Decrement counters; PermissionsBoundary at CreateUser and CreateRole creation time; SAML AssertionEncryptionMode, AddPrivateKey/RemovePrivateKey with PrivateKeyList response; ServiceSpecificCredential CredentialAgeDays and ExpirationDate; Server Certificate PrivateKey storage; DeleteServiceLinkedRole rejects non-service-linked roles; RoleLastUsed tracking on AssumeRole; MFA TOTP wider window with strict consecutive-code enforcement; transport-agnostic core functions in core.go shared between AWS API and admin gRPC-Web handlers; admin storeErr maps AWS error codes by Code string for correct gRPC status (DeleteConflict→FailedPrecondition, LimitExceeded→ResourceExhausted).
- **IoT** — Fleet indexing query engine with aggregation pipelines (count, cardinality, percentile, term buckets); MQTT broker mTLS enforcement with TLS fingerprint identity; Publish throttling and connection lifecycle tracking; GetThingConnectivityData live status; IoT policy condition operators (case-insensitive, ARN, CIDR, Numeric, Date) and version management; policy document and IoT SQL validation; authorizer tokenSigningPublicKeys with TestInvokeAuthorizer Lambda and signature verification; IoT rules substitution templates, DynamoDB actions, and SQL three-valued logic; job config (presigned URLs, abort/timeout/rollout/retry/scheduling) with SCHEDULED status; SecurityProfile metrics and tags; CreateProvisioningClaim real X.509 certificate; OTAUpdate expanded fields; Dimension type; ProvisioningTemplate preProvisioningHook.
- **Kinesis** — ExplicitHashKey shard routing on PutRecord/PutRecords; GetRecords retention filtering, ChildShards on shard closure, and Limit validation (1–10000); SubscribeToShard continuous event-stream with MillisBehindLatest and ChildShards; Stream WarmThroughput and MaxRecordSizeInKiB on CreateStream/UpdateStreamMode/UpdateStreamWarmThroughput; StartStreamEncryption validation (EncryptionType=KMS, KeyId 1–2048 chars); UpdateShardCount ScalingType validation; DescribeLimits actual shard and on-demand counts; ListStreamConsumers pagination with StreamCreationTimestamp verification and RegisterStreamConsumer Tags; ListShards StreamCreationTimestamp verification and ShardOrder; ListTagsForStream pagination; ShardLevelMetrics and PutResourcePolicy validation; ShardFilterType aligned to AWS-spec enum values; stream name format validation; ListStreams limit 10000 with opaque NextToken.
- **KMS** — Grant authorisation with explicit Deny, SourceArn/EncryptionContext constraints, and S3 SSE-KMS evaluation; DryRun on cryptographic and grant operations; RotateKeyOnDemand with versioned ciphertext for post-rotation decryption; GetKeyLastUsage; ListGrants GrantId filter; ListKeyRotations actual history; CreateKey/ReplicateKey BypassPolicyLockoutSafetyCheck, replica policy and tags; ImportKeyMaterial ExpirationModel; RSA encrypt/decrypt with plaintext caps; HMAC key material bounds; EncryptionAlgorithm resolution; tag quota enforcement; EnableKeyRotation restricted to SYMMETRIC_DEFAULT AWS_KMS; master key from environment variable.
- **Lambda** — Async invocation with EventInvokeConfig retry (validated ranges), destination delivery (OnSuccess/OnFailure), and payload size limits; DryRun invocation; reserved concurrency enforcement with zero-pause; event source mapping create-time configuration (Enabled, DestinationConfig, FilterCriteria, retry policy) with service ARN and StartingPosition validation; alias weighted routing with ListAliases pagination; layer version policy APIs, CompatibleRuntime filter, and code persistence; LoggingConfig, ImageConfig, FileSystemConfigs, and SnapStart validation on Create/Update; function URL AWS_IAM SigV4 verification; provisioned concurrency pre-warm and orphaned container cleanup; EC2 dependency checks (subnet/security-group usage).
- **Neptune Data** — StartLoaderJob accepts all AWS-spec parameters (failOnError, queueRequest, dependencies, parserConfiguration, userProvidedEdgeIds, edgeOnlyLoad) with format, mode, and parallelism enum validation; loader job queue with dependency resolution and concurrency limit; GetLoaderJobStatus returns AWS-spec response shape (feedCount list, overallStatus with fullUri, runNumber, duplicates, parsingErrors, failedFeeds); per-file progress persistence; HTTPS S3 URL normalisation; N-Quads format support; Gremlin explain and profile output as formatted text with profile.* parameters; WebSocket subprotocol negotiation (GraphBinary, GraphSON v1–v3); panic recovery and send-buffer-full force-close; engine status enriched with dbEngineVersion, dfeQueryEngine, and feature flags; GetPropertygraphStream Accept-Encoding header; 30-second statistics refresh timeout.
- **Neptune** — Create operations transition through creating→available state machines (DBCluster, global cluster, snapshot, event subscription); MasterUserPassword bcrypt-hashed and stored write-only; DBCluster output fields round-tripped through Describe (AllocatedStorage, DBClusterMembers, ReaderEndpoint, DbClusterResourceId, NetworkType, PendingModifiedValues); ServerlessV2ScalingConfiguration parsed and range-validated; ModifyDBInstance rename; ModifyGlobalCluster rename propagation; FailoverDBCluster state check; Port (1150–65535) and BackupRetentionPeriod (1–35) validation; parameter-group and subnet-group existence checks; IAM role ARN, SNS topic ARN, and EventCategories validated on create; VpcSecurityGroups output key corrected to AWS-spec name; RestoreDBInstanceToPointInTime opens per-instance engine; DeleteDBSnapshot returns deleted snapshot per AWS-spec; SubnetUsageChecker for EC2 dependency checks.
- **Neptune Graph** — StartExportTask ExportFilter with vertex/edge label filtering, per-property selection (SourcePropertyName rename, OutputType type conversion), and CSV column-key aggregation; GetGraphSummary DETAILED mode with property-level statistics (numNodeProperties, numEdgeProperties, totalNodePropertyValues, totalEdgePropertyValues, per-label nodeProperties, edgeProperties, nodeStructures with distinct outgoing edge labels, edgeStructures); ExecuteQuery explain mode (STATIC/DETAILS query plan with queryType and matchCount), queryTimeoutMilliseconds with QUERY_TIMEOUT UnprocessableException, and QUERY_TOO_LARGE 1 MiB limit; CancelExportTask and CancelImportTask transition through CANCELLING state per Smithy TaskState model with concurrent-cancel detection on all advancement paths; CancelQuery transitions to CANCELLING and terminal queries deleted per Smithy QueryState model (RUNNING/WAITING/CANCELLING only); provisionedMemory and replicaCount validation returns CONSTRAINT_VIOLATION.
- **RDS** — DBInstance and DBClusterSnapshot fields round-tripped through Describe; CreateDBSnapshot rejects terminal instance states; Filter support across Describe* handlers; DescribeEvents source-type filter workaround for proto3 zero-value ambiguity; final snapshot on Delete with DeletionProtection enforcement; subnet-group and parameter-group existence validated on create; Engine required on Create; CreateDBCluster opens per-cluster engine; engine-family-aware default parameters; AWS-spec identifier and Engine/EngineVersion whitelist with expanded MySQL (8.0.43–8.0.46, 8.4.5–8.4.10) and Neptune (1.4.3.0–1.4.7.0) versions; Neptune handler dispatches MySQL engine; **Data API**: AWS-spec exception taxonomy with dbConnectionId on StatementTimeoutException; parameter validation with SQL-injection guards and Field union enforcement (at most one discriminator); TypeHint-aware SQL substitution; response classification by engine schema; DML RowsAffected and InsertID; ResultSetOptions honoured; 1 MiB response cap; 45-second statement timeout with ContinueAfterTimeout, panic recovery, and per-transaction statement serialisation; AWS-spec transaction timeouts (idle 3 min, max-life 24 h); SecretResolver wired to Secrets Manager.
- **Route 53** — Health check configuration expansion (SearchString, InsufficientDataHealthStatus, HealthThreshold, ChildHealthChecks, RoutingControlArn, Regions, AlarmIdentifier — parse, response serialisation, UpdateHealthCheck with ResetElements); health check validation (Type enum, numeric ranges, length limits, HealthCheckVersion optimistic concurrency, CloudWatchAlarmConfiguration output); DeleteHealthCheck HealthCheckInUse guard; ListResourceRecordSets lexicographic pagination with deleted-record continuation; ChangeResourceRecordSets TTL and HealthCheckId validation on CREATE/UPSERT; ListHostedZones DelegationSetId and HostedZoneType filtering; CreateHostedZone RFC 1035 domain validation and ChangeInfo in idempotent return; cross-service VPC existence validation; DisassociateVPC LastVPCAssociation enforcement and VPCAssociationNotFound; ChangeInfo persisted on VPC associate/disassociate; tag resource existence verification, key/value limits, and 50-tag quota; DNS server synchronous bind, full-zone pagination, FORMERR, TTL=0 and SOA fail-closed; admin console VPC output and zone-not-empty deletion guard.
- **S3** — Cross-region replication (Put/Get/Delete; async object copy with prefix/tag filter, delete-marker propagation, and SSE-preserving `PutEncrypted`); CORS preflight and `Access-Control-*` header injection; lifecycle worker (expiration by Days/Date, noncurrent-version expiration, abort-incomplete-multipart, Object Lock protection, idempotent start); access control reordering (policy explicit Deny overrides ACL Allow; policy Allow checked before ACL fallback); canned ACL expansion (`bucket-owner-read`, `bucket-owner-full-control`, `aws-exec-read`); ListBuckets v2 pagination (`max-buckets`, `continuation-token`, `prefix`, `bucket-region`, `IsTruncated`); bucket configuration validation (lifecycle Days/Date mutual exclusion and ranges, StorageClass whitelist, notification Id/ARN/event validation, Object Lock Mode/Days/Years, encryption one-rule, OwnershipControls enum); GetObject `If-Match`/`If-None-Match` wildcard; CompleteMultipartUpload duplicate-part last-occurrence; ListMultipartUploads Initiator and Owner; Object Lock enforcement on DeleteObject/DeleteObjects; PutObject `x-amz-tagging`; admin console bucket name validation, BucketRegion output, and Object Lock deletion warning.
- **Scheduler** — AWS cron L/W/# wildcard support via forward-scanning evaluator; cron field-count (6) and at() semantic date validation; persisted retry records with at-least-once delivery; RetryPolicy range rejection (was silently clamped); exponential backoff with crypto/rand jitter; DeadLetterConfig routing to SQS or SNS; SNS delivery via PublishToTopic for full subscription fan-out; SQS FIFO MessageGroupId fallback to schedule name; ClientToken idempotency for CreateSchedule/CreateScheduleGroup (24h in-memory TTL); shared validation layer across HTTP API and admin console (name, expression, Target/RoleArn/DLQ ARN, KmsKeyArn KMS-service, Timezone IANA, Description 512-char, EventBridge/Kinesis sub-parameter lengths, StartDate/EndDate ordering); ActionAfterCompletion=DELETE deferred to lifecycle completion (defaults to NONE); ScheduleExpressionTimezone evaluation; FlexibleTimeWindow Mode/range; tag resource existence verification; engine context propagation for retry shutdown; store lifecycle cleanup on engine stop.
- **Secrets Manager** — resource-policy validation framework: PutResourcePolicy BlockPublicPolicy (PublicPolicyException), MalformedPolicyDocumentException for malformed JSON, and ValidateResourcePolicy multi-check (syntax + missing version, all issues at once); CreateSecret inline AddReplicaRegions with ForceOverwriteReplicaSecret and shared replication engine; ClientRequestToken across CreateSecret/UpdateSecret/PutSecretValue/RotateSecret (32–64 chars, version ID override, rotation cycle token); RotateImmediately (defaults true); PutSecretValue RotationToken (36–256 chars); BatchGetSecretValue Filters mode (SecretIdList mutual exclusion, MaxResults 1–20 pagination); ListSecretVersionIds IncludeDeprecated filtering, MaxResults 1–100 pagination, LastAccessedDate/KmsKeyIds output; ListSecrets default SortBy=name; UpdateSecretVersionStage three-mode (add/remove/move) with self-move rejection; metadata-only UpdateSecret; name (1–512) and description (0–2048) length validation; tag quota enforcement (max 50, key 1–128, value 0–256, merged-total check on TagResource); admin handler parity (CreateSecret validation, DeleteSecret recovery window/force-delete, ListSecrets default 100).
- **SESv2** — BYODKIM on CreateEmailIdentity and PutEmailIdentityDkimSigningAttributes (response carries DkimStatus, DkimTokens, SigningHostedZone); SendEmail/SendBulkEmail expanded (IdentityArn for From/FeedbackForwarding, ListManagementOptions, DefaultEmailTags merged with ReplacementTags, sender and recipient address validation); TenantName rejection across email and suppression operations; GetEmailIdentity returns VerificationStatus, VerificationInfo, Tags, and Policies; GetContactList returns CreatedTimestamp and Tags; GetContact returns ContactListName, CreatedTimestamp, TopicDefaultPreferences; GetEmailTemplate returns Tags; GetSuppressedDestination returns Attributes; GetAccount ReviewDetails (GRANTED) with nil-safe guards; suppression expansion (SuppressionScope, ConditionThreshold at account and config-set level); PutAccountVdmAttributes VdmEnabled enum validation; input validation (identity format, ContactListName pattern, AttributesData JSON, Reason BOUNCE/COMPLAINT); unsupported Handlebars syntax detection; ListSuppressedDestinations and ListContacts filters; ListEmailIdentities PageSize cap; GetConfigurationSet and CreateDedicatedIpPool Tags; ProductionAccessEnabled nullable semantics; DKIM attributes expanded; contact-list topic field validation.
- **SNS** — MessageBody filter policy scope; subscription confirmation sent to HTTP/HTTPS endpoints; raw message delivery for HTTP and Lambda (was SQS-only); Publish/PublishBatch parameter validation (message 256 KB, subject 100-char, MessageStructure=json with default-key enforcement, message attribute DataType enum, FIFO-only params rejected on standard topics); PublishBatch duplicate ID detection, total size including attributes, and empty-batch rejection; topic attribute validation (DeliveryPolicy/Policy JSON, DisplayName 100-char, KmsMasterKeyId UUID/alias format); platform application validation (platform enum, name 100-char, token/customUserData 2048-byte); AddPermission account ID (12-digit numeric) and action name whitelist; non-consecutive FIFO sequence numbers; topic name character and length validation.
- **SQS** — Per-queue FIFO sequence numbers; AWSTraceHeader system attribute support with MD5OfMessageSystemAttributes; SendMessageBatch two-pass pre-validation (all entries validated before any send); message attribute DataType validation (String/Number/Binary with custom suffix); BatchRequestTooLong cumulative size enforcement; AddPermission JSON-array and ActionName.N parsing with account ID (12-digit), action whitelist, and label count (max 10); queue attribute validation (Policy JSON, KmsDataKeyReusePeriodSeconds, DeduplicationScope, FifoThroughputLimit, SqsManagedSseEnabled, RedriveAllowPolicy, KmsMasterKeyId format); SetQueueAttributes rejects FifoQueue and ContentBasedDeduplication mutation; FIFO queue name bidirectional validation (.fifo suffix requires FifoQueue=true and vice versa); message move task background transfer with rate limiting, FIFO dedup and sequence assignment, atomic copy+delete, source/destination existence checks, ApproximateNumberOfMessagesToMove count, MaxResults pagination, and terminal-state cancel rejection; ReceiveRequestAttemptId FIFO receive deduplication; DLQ SentTimestamp semantics (standard queues preserve original, FIFO queues reset per AWS spec).
- **SSM** — ParameterFilter (ParameterStringFilter Key/Option/Values) with Smithy-spec key validation and operation-specific exclusion rules for DescribeParameters and GetParametersByPath; PutParameter validation shared across HTTP and admin (normalisePutParameter: KeyID pattern, AllowedPattern regex, DataType enum, Tier enum with Standard→Advanced auto-promotion on >4 KB or Policies, Policies JSON type/Version validation); AllowedPattern value enforcement on both creation and overwrite; GetParameters version and label selectors (:N, :label); LabelParameterVersion 10-label cap with InvalidLabels; label, hierarchy-path, MaxResults, and ParameterNameList (max 10) validation; LastModifiedBy tracking, Policies output (ParameterInlinePolicy shape) in DescribeParameters, and GetParameterByVersion expanded fields; ResourceType validation on tag operations.
- **Step Functions** — State definition expansion (Choice operators incl. *Path variants, Wait Timestamp/TimestampPath, Map/Parallel ResultPath, TaskStateEntered/Exited, Retry cap); activity heartbeat enforcement (HeartbeatSeconds, SendTaskHeartbeat); SQS/SNS integration attribute passthrough; RevisionId optimistic concurrency; encryptionConfiguration (AWS_OWNED_KEY/CUSTOMER_MANAGED_KMS_KEY) and tracingConfiguration; publish flag and STANDARD/EXPRESS type validation on Create/UpdateStateMachine; MapRun itemCounts/executionCounts restructure with Smithy-aligned response shapes; execution API expansion (redriveStatus, version/alias ARNs, redriveCount/Date; ListExecutions mapRunArn/redriveFilter; GetExecutionHistory includeExecutionData/reverseOrder); ValidateStateMachineDefinition per-state diagnostics, severity filter, maxResults; CreateActivity name/tags/encryptionConfiguration; ExecutionAlreadyExists and TaskDoesNotExist errors; maxResults and routingConfiguration weight validation.
- **STS** — MFA TOTP verification with condition key injection; session tags with transitive propagation and privilege-escalation prevention; AssumeRole validation (ExternalId, RoleSessionName, SourceIdentity, sessionPolicyDocumentType/unrestrictedSessionPolicyDocumentType, PolicyArns arnType); AssumeRoleWithSAML/WebIdentity validation (SAMLAssertionType, urlType, contextAssertionType, ProvidedContextsListType) with Conditions/NotOnOrAfter and exp-claim expiry enforcement; root-user duration cap, MaxSessionDuration, and SessionDurationEscalationException; GetWebIdentityToken JWT signing (RS256/ES384) with tag injection; ASIA temporary-credential authorisation (session-policy intersection, deny-closed, aws:PrincipalTag/*, SourceIdentity as sts:SourceIdentity); PackedPolicySize overhead for ARNs, tags, and transitive keys; background TTL sweeper with ExpiredTokenException (HTTP 400) SDK propagation; GetAccessKeyInfo key-existence validation across IAM and STS stores.
- **Timestream Query** — ProvisionedCapacity account settings (TargetQueryTCU, NotificationConfiguration with SNS); ComputeMode/QueryPricingModel enum validation; scheduled-query target configuration expansion (DimensionMappings, MultiMeasureMappings with TargetMultiMeasureName, MixedMeasureMappings, MeasureNameColumn); PrepareQuery ValidateOnly mode and parameter type inference from :: cast operator with deduplication; Query QueryId as alphanumeric per Smithy; CancelQuery QueryId format validation; ExecuteScheduledQuery InvocationTime and QueryInsights; RecentlyFailedRuns with per-run trigger attribution; admin console ListScheduledQueries enrichment (State, LastRunStatus, ErrorReportConfiguration, TargetDestination); S3ErrorReportConfiguration EncryptionOption.
- **Timestream Write** — MagneticStoreWriteProperties (EnableMagneticStoreWrites, MagneticStoreRejectedDataLocation with S3Configuration); batch load real S3 execution with CSV parsing, resume support, and processed-key tracking (replaces simulation); BatchLoadTask data model expansion (MixedMeasureMappings, MultiMeasureMappings with TargetMultiMeasureName); ClientToken idempotency for CreateBatchLoadTask; BatchLoadTaskId server-generated uppercase alphanumeric per Smithy; WriteRecords RejectedRecordsException (HTTP 419) with RejectedRecords array; WriteRecords MagneticStore count reflects EnableMagneticStoreWrites; AWSError RawFields support for complex error payloads; admin console ListTables enrichment (TableStatus, RetentionProperties, Schema).
- **WAFv2** — Statement-tree enum validation (PositionalConstraint, ComparisonOperator, TextTransformationType, OversizeHandling, MapMatchScope, AggregateKeyType) returning WAFInvalidParameterException; LoggingFilter parsing and serialisation with ActionCondition/LabelNameCondition, LogScope/LogType enum validation; WebACL and RuleGroup model expansion (CustomResponseBodies, CaptchaConfig, ChallengeConfig, TokenDomains, AssociationConfig, MonetizationConfig, DataProtectionConfig, OnSourceDDoSProtection, LabelNamespace, AvailableLabels, ConsumedLabels); RuleLabels/CaptchaConfig/ChallengeConfig on Rule; regex pattern compilation validation on Create/Update; scope-aware duplicate detection (same name permitted across CLOUDFRONT/REGIONAL); four additional managed rule groups, DescribeManagedRuleGroup SnsTopicArn/ConsumedLabels, dynamic version names; ApplicationIntegrationURL in GetWebACL.
- **pkg/vsjwt** — Manager constructors validate key presence and minimum 2048-bit RSA size; PKCS8 PEM private-key format supported alongside PKCS1; `MustNewManager`/`MustNewManagerWithPublicKey` convenience wrappers for test and initialisation code; token validation requires `exp` claim and restricts signing algorithm to RS256.
- **pkg/vtl** — Nested block processing (`#foreach`, `#if`) now runs the full 8-phase pipeline (control flow, AppSync util, input, util, AppSync context, toJson, context, stage variables), enabling `$input.*`, `$util.*`, and `$stageVariables.*` resolution inside loop and conditional bodies; JSON marshalling errors captured and propagated via `Transform` return (was silently swallowed).
- **Admin authentication** — gRPC-Web interceptor and HTTP middleware now enforce `token_use=access`, rejecting ID tokens for admin API requests.

### Fixed

- **ACM**: SignatureAlgorithm now uses AWS format (SHA256WITHECDSA etc., were ECDSAWITHSHA*); KeyAlgorithm/SignatureAlgorithm extraction now covers all RSA/ECDSA variants (were missing several); enum parameters now reject invalid values (were silently defaulting); PrivateKey limit now 5 KB (was 512 KB); SANs now truncated to 100 in summaries (were full list); Exported and SortBy=EXPORTED now consistently use WasExported (were PrivateKey presence / Options.Export); ResendValidationEmail now restricted to PENDING_VALIDATION (was allowing ISSUED); error types aligned to Smithy; ARN validated as 6-part structure; decodeBase64PEM returns ValidationException on invalid base64 (was silently accepting); Serial matches X.509 SerialNumber for import/new/renew (was MD5 hash); Subject/SAN filter rejects unknown ComparisonOperator (was defaulting to CONTAINS); RenewalStatus filter false on nil RenewalSummary (was matching empty string); RSA_1024 rejected at issuance (was silently generating RSA_2048).
- **API Gateway**: quota fails closed and rejects unsupported periods (was fail-open, empty as DAY); Lambda authoriser honours Deny/globs, syncs Host for SigV4, preserves TTL=0 (was 300); API key required returns 403 (was 401); DynamoDB Action required, JSON errors propagated; DeleteResource 500 on internal errors (was 404); stage variable substitution single-pass (was recursive); patch move/copy/test rejected (were accepted); BasePathMapping rename restores on failure (was data loss); deployment rollback on CreateStage failure (was orphaned); throttlers cleaned up on DeleteStage (was leaking); X-Forwarded-For loopback-only (was any), access log CR/LF sanitised; GetUsage rejects invalid dates (was Feb 31); CreateModel contentType now required (was defaulting to application/json); authoriser TTL validation consistent across REST/admin (admin was missing upper-bound, REST was treating explicit 0 as unset); admin CreateRestApi negative minimumCompressionSize now rejected (was silently ignored).
- **AppSync**: `$util.appendError` no longer halts resolver execution (now returns partial data alongside collected errors); DomainName HostedZoneId returns CloudFront zone ID; EventBridge event source corrected; ApiCache initial status CREATING with async AVAILABLE; StartSchemaMerge returns MERGE_IN_PROGRESS with async completion; EvaluateCode/EvaluateMappingTemplate return BadRequestException on missing inputs; UpdateGraphqlApi/UpdateApi required fields enforced; UpdateApiCache TTL distinguishes absent from 0; schema creation status persisted with retry backoff; GraphQL HTTP status now maps INTERNAL_FAILURE to 500 via errorType field (was comparing message string — never matched); GraphQL subscription operations now rejected on HTTP endpoint (was attempting synchronous resolution); pagination default page size now 25 (was 50 — exceeded Smithy @range max); tag resource ARN validation default case now rejects non-apis/ resources (was accepting arbitrary paths); ListResolversByFunction pagination uses opaque store markers (was decimal offset — tokens invalidated on data change); UpdateDomainName returns NotFound for missing domain (was silent upsert); async merged-API disassociation persists DELETION_FAILED status on delete failure (was stuck in DELETION_SCHEDULED); parseAuthModes rejects invalid authType values (was silently storing unknown modes); admin CreateGraphqlApi unknown authentication type now rejected (was defaulting to API_KEY); ApiCache TTL validated 1–3600 on Create/Update (was unbounded); DeleteChannelNamespace removes stale WebSocket subscriptions (was leaving phantom subscriptions delivering events on deleted namespaces).
- **Athena**: StopQueryExecution rejects non-cancellable states; capacity reservation not-found returns InvalidRequestException; S3 result output restricted to DML; getTableData failures propagate as errors; DataScannedInBytes from actual cell lengths; CREATE TABLE no longer eagerly creates empty data; LOCATION parsing uses word-boundary match; evaluateWhere propagates errors for unsupported types, WHERE, HAVING, and JOIN ON (was fail-open/swallowed); executeSQLQuery returns TABLE_NOT_FOUND (was returning empty result set); executeDropTable propagates DeleteTableData errors (was silently logged); store Get/AppendRows distinguish ErrNotFound from other errors (were all mapped to sentinel); MaxResults=0 treated as default (was causing infinite pagination loop); GROUP BY key uses length-prefix encoding (was pipe-delimited, collision-susceptible); INSERT uses AppendRows with key lock (was non-atomic); admin ListWorkGroups default 50 (was 100); DeleteWorkGroup cascade cleanup shared between HTTP and admin.
- **CloudFront**: distribution config response now serialises all association and forwarded-values Items (were hardcoded to empty defaults); ListCachePolicies, ListOriginRequestPolicies, and ListResponseHeadersPolicies return full config; ListDistributions returns full DistributionSummary; invalidation status casing corrected; CreateInvalidation rejects Quantity/Items mismatch; OriginAccessControlSummary field name corrected; MinimumProtocolVersion default TLSv1.2_2021; OriginSslProtocols restricted to TLSv1.2; X-Forwarded-For strips port.
- **CloudTrail**: UpdateTrail now uses existence checks so empty strings clear fields; CreateTrail requires S3BucketName; DeleteTrail resolves trail ARN; DeleteEventDataStore soft-deletes; StartQuery executes asynchronously; CancelQuery rejects already-cancelled; ListQueries paginates with NextToken; GetResourcePolicy returns ResourcePolicyNotFoundException when absent; PutResourcePolicy validates ARN for EDS and channels; CreateChannel tag parameter corrected; BillingMode default corrected; tag validation moved before create; LookupEvents rejects excess attributes and MaxResults.
- **CloudWatch**: ComparisonOperator name corrected to AWS-spec; statistic matching now case-insensitive (was exact-match); alarm mute rule status recomputed at read time; anomaly detector response emits SingleMetricAnomalyDetector nested object (was flat); DescribeAnomalyDetectors parameter name corrected to plural; DescribeAlarmHistory filters applied during iteration with global sort; pagination parameter names corrected to NextToken/MaxResults across all operations (was Marker/MaxItems); PutDashboard tags ignored on update per Smithy; GetDashboard response trimmed to Smithy fields.
- **CloudWatch Logs**: FilterLogEvents startFromHead default corrected to true; event ordering and pagination stabilised (was non-monotonic across pages); DescribeLogStreams orderBy=LastEventTime now sorts globally before paginating; GetLogEvents tokens now pure numeric offsets; per-event ingestionTime persisted via chunk V2; PutLogEvents processing moved to background goroutine; MetricFilterCount atomic; PolicyType enum corrected to Smithy-spec values; StopQuery returns success boolean per Smithy; DescribeQueries createTime now epoch milliseconds; ImportStatus corrected to IN_PROGRESS; ImportStatistics tracks bytesImported; TriggerHistoryRecord field names corrected to Smithy; CreateImportTask derives managed log group name from source ARN; filter policy mutual exclusion enforced.
- **Cognito Identity**: GetCredentialsForIdentity now issues real STS sessions (was fabricated UUID credentials); GetOpenIdToken now returns signed JWT (was raw UUID); GetId reuses existing identity for matching logins; AllowClassicFlow and ServerSideTokenCheck always present in responses; TokenDuration validated against AWS-spec range; SetIdentityPoolRoles requires Roles parameter with RoleMapping Type/AmbiguousRoleResolution/MatchType enum validation; GetOpenIdToken no longer persists Logins (security hardening); LookupDeveloperIdentity and ListIdentities MaxResults >60 rejected per Smithy (was clamped); UnlinkIdentity validates provider name and token value (was name-only); UpdateIdentityPool enforces required IdentityPoolName and AllowUnauthenticatedIdentities per Smithy; all internal fmt.Errorf replaced with AWS-spec exceptions; tag operations on non-existent pools return ResourceNotFoundException.
- **Cognito IDP**: AdminDeleteUser now deletes all tokens before user record (was orphaning); Lambda triggers now propagate ClientMetadata/ValidationData (was nil); USER_SRP_AUTH now issues PASSWORD_VERIFIER challenge (was InvalidParameterException); GlobalSignOut deletes ID tokens alongside access/refresh; session IDs now full UUID (was truncated); UpdateUserPool preserves unset PasswordPolicy/LambdaConfig (was resetting to defaults); CreateUserPool enforces AliasAttributes/UsernameAttributes mutual exclusion; GetUserAuthFactors returns Smithy AuthFactorType enum values (was non-standard names); SetUICustomization base64-decodes ImageFile (was raw string as bytes); CreateTokens parameterises PreTokenGeneration triggerSource per call path (was hardcoded, causing double-trigger and discarded claims on refresh); determineDeliveryMedium defaults to EMAIL (was empty); AdminCreateUser validates DesiredDeliveryMediums before persisting (was after creation); ListWebAuthnCredentials MaxResults corrected to Smithy cap; branding asset parser validates against Smithy enums and file magic bytes.
- **DynamoDB**: condition expressions now resolve nested document paths; importDynamoDBJSONData updates GSI indexes atomically; BatchWriteItem dispatches Kinesis records; error HTTP status codes aligned to Smithy; TTL and Kinesis streaming return intermediate states; ListBackups honours pagination; ListGlobalTables incremental pagination; DescribeStream StreamStatus reflects actual state; batch/transaction operations reject malformed items; GetRecords Limit validated; PutResourcePolicy RevisionId monotonic; ImportTable rejects missing KeySchema; splitByLogicalOp word-boundary matching.
- **EC2**: `RevokeSecurityGroupIngress`/`RevokeSecurityGroupEgress` reused the Authorize-side IP validation rules, rejecting legitimate revokes (now `modifySecurityGroupRules` takes a `validateRules bool` parameter: Authorize passes true, Revoke passes false).
- **EventBridge**: PutEvents now respects entry Time field; matchOperator fails on malformed input; exists:false matches absent fields; cascade-delete paginates all rules/targets and aborts on failure; replay panic logged and marked FAILED; archive SizeBytes uses JSON byte length; ListConnections parameter corrected to ConnectionState; CancelReplay returns IllegalStatusException; ListRuleNamesByTarget NextToken validated; UpdateArchive mutex-locked; PutRule/TestEventPattern return InvalidEventPatternException; DeleteConnection returns pre-deletion snapshot; PutTargets non-map entry returns failure entry.
- **IAM**: DeleteGroup/DeleteUser return conflict errors for dependent resources (was silently cascade-deleting); RenameUser restructured to two-phase migration with rollback; error HTTP status codes aligned to Smithy; credential report password fields populated from login profile and policy; MFA Base32StringSeed base64-encodes the UTF-8 bytes of the base32 seed string (matches AWS wire format); TOTP consecutive validation requires adjacent steps; PermissionsBoundaryUsageCount via counter on Put/Delete/cascade (was N+1 scan per policyToResponse and leaked on entity delete); policy statements reject Action+NotAction and Resource+NotResource co-presence (was accepting both); hasPolicyKey treats empty strings/arrays/maps/null as absent (was fail-OPEN); DeleteAccountAlias returns NoSuchEntity for mismatched alias; DeleteServiceLinkedRole rejects non-SLR roles; OIDC thumbprint validates hex (was length-only); entity-level LastAuthenticated computed from action access dates (was job creation time); admin DeletePolicy now checks AttachmentCount and cleans up versions via deletePolicyCore (was bare Delete with no attachment or version checks); admin storeErr differentiates DeleteConflict from EntityAlreadyExists (was mapping all HTTP 409 to CodeAlreadyExists); NoSuchEntity error messages use lowercase entity types per AWS convention (were capitalised); UploadServerCertificate Tags round-tripped (was dropped) and PrivateKey validated as required (was silently ignored); AttachUserPolicy/AttachRolePolicy accept AWS-managed policy ARNs (was customer-managed only).
- **IoT**: MQTT broker auth hook now fail-closed (was allowAllHook permitting all connections); policy lookups use full principal ARN (was raw cert ID, failing all enforcement); iot:Connect resource matching uses AWS policy wildcards (was MQTT wildcards); connection tracker resolves cert ID from TLS fingerprint exclusively (was MQTT Username, allowing spoofing); UpdateAuthorizer keys only overwritten when provided; GetOTAUpdate tags assertion corrected for JSON round-trip; jobResponse fields JSON-validated; error codes aligned to Smithy (VersionsLimitExceededException, CertificateStateException, VersionConflictException).
- **Kinesis**: GetRecords MillisBehindLatest now calculated from last record's ApproximateArrivalTimestamp (was hardcoded 0); PutRecord and PutRecords now validate data size against base64-decoded payload length using the stream's MaxRecordSizeInKiB (was checking base64-encoded length against hardcoded 1 MiB); PutRecord no longer rejects empty Data (AWS allows empty-data records); UpdateMaxRecordSize upper bound raised to 10240 KiB (was 1024); SplitShard child hash key ranges now contiguous with no overlap or gap (was overlapping at the split boundary for both auto-midpoint and user-supplied NewStartingHashKey); SubscribeToShard now streams continuously with proper event framing (was single-shot read returning only the first batch).
- **KMS**: Sign/Verify/GenerateMac/VerifyMac now require base64 (was raw-bytes); CreateGrant validates Operations and GrantName; GrantConstraints in PascalCase; RevokeGrant/RetireGrant return ValidationException for missing required; PendingWindowInDays uses existence check; ImportKeyMaterial restores pre-import state and honours ExpirationModel; Decrypt validates algorithmSupported; key-state transitions per AWS table; ListKeyRotations returns actual history (was empty); ScheduleDeletion rejects double-scheduling; principal resolution returns sentinel ARN (was granting root); grant not-found returns NotFoundException (was generic error).
- **Lambda**: PublishVersion now snapshots LoggingConfig, ImageConfig, FileSystemConfigs, and signing attributes into version records; copyCodeToContainer rejects invalid deployment packages (was silently writing raw bytes as fallback); ESM Enabled=false honoured on Create (store was unconditionally overriding); layer version CodeLocation persisted and returned by GetLayerVersion (was in-memory only); ESM filtered-out records acknowledged on all sources (was infinite re-polling); async retry uses detached context (was inheriting HTTP context, killing post-202 retries); filter exists:false OR semantics for absent keys (was single-criterion only); resolveVpcConfig propagates subnet errors (was silently swallowing).
- **Neptune**: clusterIDFromARN preserves colons in identifiers (was truncating); DBCluster/DBInstance new fields persisted through protobuf (was in-memory only); DBClusterMembers tracked through instance lifecycle (was absent from Describe); ModifyGlobalCluster deletes old entry before member update (was stale references); DeleteDBCluster rolls back on failure (was stuck in deleting); VpcSecurityGroups status derived from cluster lifecycle (was hardcoded active); error type InvalidDBParameterGroupStateFault (was InvalidParameterGroupState); AddRole/RemoveRole raise AWS-spec faults; snapshot operations use MissingParameter error type; Describe handlers paginate with DefaultEngineVersion fallback; PromoteReadReplicaDBCluster removes from global members; RestoreDBClusterFromSnapshot validates cluster ID and engine.
- **Neptune Data**: loader store operations use cluster region for store lookups (was S3 bucket region, causing jobs to silently stall in LOAD_IN_PROGRESS); GetClusterEngineForRegion filters by region (was returning random engine); purgeExpiredQueries covers LOAD_CANCELLED_DUE_TO_ERROR and LOAD_FAILED_BECAUSE_DEPENDENCY_NOT_SATISFIED (was leaking terminal jobs); dispatcher re-reads job from store before LOAD_IN_PROGRESS transition (was overwriting concurrent CancelLoaderJob); pagination suppresses expected limit-reached warnings via sentinel error (was logging on every paginated response); loader completion preserves concurrent CancelLoaderJob status; loadNTriples returns cancelled status; ExecuteFastReset token map uses type guards and purges expired entries; nil slices normalised to empty arrays per AWS-spec; removed dead WebSocket session-cleanup constants.
- **Neptune Graph**: import task exits IMPORTING on all failure paths including concurrent cancel during INITIALIZING; import status uses IMPORTING (was IN_PROGRESS); CancelExportTask and CancelImportTask detect concurrent cancel on all advancement paths (was stuck in CANCELLING indefinitely); CancelQuery uses Smithy-compliant CANCELLING state and deletes terminal queries (was storing undocumented COMPLETE/FAILED/CANCELLED states); CreateGraph returns 500 on store failure (was silently returning AVAILABLE); DeleteGraph returns 500 on store failure and skips duplicate snapshot on retry; endpoint operations reject empty identifiers; snapshot operations persist CREATING before copy and transition on outcome; edge-only CSV imports pre-populate node ID map from existing DB nodes; CreateGraph and RestoreGraphFromSnapshot persist FAILED on engine failure; ListSnapshots and ListExportTasks pagination corrected.
- **RDS**: identifier validation at every catalogue entry point (was allowing key-namespace collisions); NULL primary keys rejected; TIMESTAMP/DATE primary-key encodings supported; unique-index NULL semantics honour SQL standard; DropDatabase atomic via single Pebble batch; TruncateTable returns 0 rows per MySQL spec; ARN builder uses AWS-spec colon separator; Data-API database handles not cached; unknown engine returns nil instead of Neptune fallback; DescribeDBEngineVersions and validator share single source; snapshot data capture fail-closed on failure (was silently logging warning and leaving orphaned metadata); DeleteDBInstance propagates engine.Close errors (was silently ignoring); purgeExpired acquires per-transaction mutex before rollback (was racing with in-flight ExecuteStatement on shared sql.Context).
- **Route 53**: recordSetToResponse now serialises MultiValueAnswer and TrafficPolicyInstanceId (were parsed on input but omitted from responses, breaking round-trip); Weight display condition refined to show only when SetIdentifier present without other routing policies (was `Weight > 0`, omitting Weight=0 for weighted records and spuriously showing Weight=0 for latency/failover/geo/multivalue records); ChangeResourceRecordSets malformed elements now rejected (was silently skipped via `continue`); missing record-set Type now rejected (was silently accepted); DNS server bind failures now returned synchronously (was silently logged while `started` remained true).
- **S3**: versioning cache now waits for ristretto `Wait()` before returning (was allowing stale reads after status change); access control now evaluates bucket policy before ACL so explicit Deny overrides ACL Allow (was ACL-first, granting access the policy denied); `policyContainsPublicAccess` now filters by Effect=Allow and analyses Condition restrictiveness (was matching any statement with public Principal); `ParseGrantHeaders` now returns `InvalidArgument` on unparseable grantee values (was silently skipping); `PutBucketEncryption` now requires exactly one rule (was accepting zero or multiple); `DecryptWithCustomerKey` now errors on nil SSE metadata (was returning plaintext as-is).
- **Scheduler**: schedules configured for multiple regions fired once per region because last-fired dedup keys omitted the region (now keys include the region with a single cleanup sweep); rate-expression schedules with a future StartDate fired immediately (now guarded by StartDate check); last-fired slot was reserved before goroutine spawn, leaving a "fired but never ran" leak on panic (now reservation happens inside the goroutine with release on err/panic); UpdateSchedule now defaults ActionAfterCompletion to NONE on empty input (was zero value, omitting the field from responses); DeleteSchedule and DeleteScheduleGroup now delete the primary resource before best-effort tag cleanup (was tag I/O failure blocking resource deletion).
- **Secrets Manager**: DeleteSecret validates RecoveryWindowInDays (7–30) and rejects ForceDeleteWithoutRecovery combination and deletion of replicated primaries (admin handler now returns Deletiondate); PutResourcePolicy resolves secret before policy validation (ResourceNotFoundException takes precedence) and returns MalformedPolicyDocumentException for malformed JSON (was ValidationException); replica status uses PascalCase "Failed" (was "FAILED") and reflects version-sync failures (was always "InSync"); RemoveRegionsFromReplication retains entries on deletion failure (was silently dropped); StopReplicationToReplica and RestoreSecret update LastAccessedDate/LastChangedDate; rotation Lambda-not-found returns ResourceNotFoundException (was InvalidRequestException); PutSecretValue requires at least one of SecretString/SecretBinary; DeletedDate in DescribeSecret set to scheduled date per AWS-spec; nil pointer panics prevented in rotation engine and async destination delivery; failed rotation save/restores LastRotatedDate/NextRotationDate; AWSCURRENT staging label cannot be removed without target version; error mapping unified (fmt.Errorf → mapStoreError).
- **SESv2**: SendBulkEmail now rejects the entire batch when any entry contains an invalid recipient address (was silently dropping the entry); ListContacts filtered scan now capped at 50,000 entries with error return (was silently truncated at 10,000); PutEmailIdentityDkimSigningAttributes reads nested SigningAttributes; PutConfigurationSetVdmOptions reads nested VdmOptions; ArchivingOptions corrected to ArchiveArn-only; SuppressionOptions JSON tag fixed; MailFromDomainStatus initial PENDING; ScalingMode default STANDARD; DKIM key length RSA_2048_BIT; template operations reject contentless templates; TestRenderEmailTemplate Content-Type derived from content; UpdateContactList/UpdateContact distinguish absent from empty; CreateConfigurationSetEventDestination requires MatchingEventTypes; PutAccountDetails merges preserving unset fields.
- **SNS**: malformed message attributes now rejected at Publish (was silently skipped); MessageStructure=json now validated at Publish with default-key requirement (was silently delivering raw JSON); FilterPolicyScope now accepts only MessageAttributes/MessageBody (was accepting invalid MessageBodyAttributes); error codes corrected (FilterPolicyLimitExceeded/403, EmptyBatchRequest, TooManyEntriesInBatchRequest); topic and subscription structs consolidated to Attributes-map single source of truth with typed accessors (was duplicated fields); PublishToTopic message ID now UUID (was UnixNano); filter policy now enforced before each delivery; failed deliveries routed to subscription DLQ via RedrivePolicy; FilterPolicy/FilterPolicyScope/RedrivePolicy validated at Subscribe and SetSubscriptionAttributes; `anything-but` now requires attribute presence; invalid filter policy JSON fails closed; signing certificate serial now random 128-bit (was hardcoded 1).
- **SQS**: `maxReceiveCount` now honoured correctly when moving to DLQ (was `>=`, delivering one fewer receive than configured); `ReceiveMessage` now filters `MessageAttributes` and system `Attributes` per `MessageAttributeNames` / `AttributeNames` / `MessageSystemAttributeNames` (was returning all unconditionally); specifying both `AttributeNames` and `MessageSystemAttributeNames` now returns `InvalidParameterCombination`; background cleanup goroutine deletes messages exceeding `MessageRetentionPeriod` (storage hygiene; receive-time filter was already correct); store `Close()` registered as shutdown hook; SendMessageBatch now pre-validates all entries before any send (was partial-send then abort); SendMessageBatch returns `MD5OfMessageSystemAttributes` for system attributes (was omitted); DeleteMessage, ChangeMessageVisibility, and batch variants now return proper error codes (was hardcoded `ReceiptHandleIsInvalid`); CancelMessageMoveTask rejects terminal-state tasks (was overwriting to CANCELLING); message move dedup-hit deletes source message (was infinite re-listing).
- **SSM**: `DeleteParameter` masked the underlying store error behind `awserrors.NewInternalErrorException`, losing the original cause (now logs the original error via `logs.Error` before returning the AWS error); `LastModifiedUser` now populated from stored access-key identity (was hardcoded "N/A" across GetParameter, GetParameters, DescribeParameters, and GetParameterHistory); `Policies` now serialised as ParameterInlinePolicy in DescribeParameters output (was always an empty list); admin console gRPC errors now map specific store sentinels to connect codes (was generic `StoreErrorToGRPC` losing error types).
- **Step Functions**: StopExecution on terminal-state execution now returns original StopDate (was returning current time); execution FAILED-status persistence retries with fresh context (was silently dropping when caller context cancelled); redrive goroutine now logs panics with structured context (was generic RecoverPanic); MapRun response shapes now match Smithy (was returning stale flat itemCount/itemsProcessedCount/itemsFailedCount); `States.ALL` retry/catch now excludes `States.DataLimitExceeded` and `States.RuntimeExceeded` (was matching all errors).
- **STS**: resolveCallerIdentity falls back to STS sessions (was IAM-only); GetCallerIdentity SAML sessions use RoleSessionName for ARN construction (was PrincipalName); isRoot detection uses suffix ARN match (was substring); AssumeRole transitive tags propagated only with matching values (was orphaning); enforceRoleMaxSessionDuration defaults to 3600 when unset (was skip); SAML/JWT expiry fail-closed on missing NotOnOrAfter/exp and boundary uses !Before per spec (was lenient/After); extractJWTClaim array returns first element (was join-all); PackedPolicySize ceiling division (floor let 2049-byte policy pass); verifyCallerMFA fail-closed without TOTP seed (was accepting any code); GetAccessKeyInfo key-existence validation and compliant Status field; orphaned access-key index cleanup on session expiry; SourceIdentity omitted when empty (was empty string); GetOrCreateStoreE goroutine leak fixed (loser store closed); Close() and shutdown hooks for STS, DynamoDB, and Timestream.
- **Timestream Query**: error codes aligned to AWS spec — SQL parse/SELECT-only/PrepareQuery → ValidationException, schema errors → QueryExecutionException, duplicate-name → ConflictException (were fmt.Errorf or non-spec InvalidQueryException/ResourceAlreadyExistsException); CreateScheduledQuery validates Name (1-64), ClientToken (32-128), and KmsKeyId length (1-2048); CancelQuery validates QueryId format; UpdateScheduledQuery rejects non-ENABLED/DISABLED states; UpdateAccountSettings clears ProvisionedCapacity on ON_DEMAND switch; MultiMeasureMappings TargetMultiMeasureName now preserved (was silently dropped); RecentlyFailedRuns RunStatus derived from per-run TriggerType (was hardcoded); PrepareQuery phantom QueryId/QueryParsingStatus removed; ExecuteScheduledQuery uses AWS-spec run-status values (was SUCCESS/FAILED); pagination caps honoured across Query/List/ListTagsForResource; DescribeScheduledQuery LastRunSummary, ExecutionStats, and RecentlyFailedRuns populated; LIKE case-sensitive per SQL; phantom ScheduledQueryStatus constants removed.
- **Timestream Write**: ResourceAlreadyExistsException → ConflictException (CreateDatabase, CreateTable, CreateBatchLoadTask); KmsKeyId length (1-2048), ClientToken length (1-64), and DataFormat enum validation; TaskId pattern validation on Describe/ResumeBatchLoadTask; batch CSV parsing preserves TargetMultiMeasureName and handles MixedMeasureMappings (were dropped/ignored); batch load region from request context (was hardcoded); ListBatchLoadTasks field names DatabaseName/TableName (were Target*); phantom DeleteBatchLoadTask removed; WriteRecords validates batch size (1–100) and merges CommonAttributes; retention properties enforce required traits and ranges; ResumeBatchLoadTask uses atomic compare-and-swap; list operations honour pagination caps; RecordsIngested.Total reports actual count; parseTimestamp uses strict parsing; empty MeasureValueType defaults to DOUBLE; unused store-interface declarations deleted.
- **WAFv2**: ValidationException → WAFInvalidParameterException across all handlers; LockToken now required on Delete/Update (was accepted when empty); CreateWebACL Capacity no longer from request (read-only per AWS); GetWebACL Scope removed (absent from AWS SDK WebACL type); removed GetChangeToken/GetChangeTokenStatus (WAF v1); CheckCapacity computes actual WCU across all sixteen statement types (was hardcoded 10); DisassociateWebACL takes ResourceArn only per Smithy; Scope/DefaultAction validation enforces terminating actions; ListResourcesForWebACL filters by ResourceType; LockToken now UUID per Smithy; Statement union expanded to sixteen members, FieldToMatch to fourteen variants; store-layer Delete made atomic; field names aligned to Smithy PascalCase; association keyed by ResourceArn; CreateRuleGroup Capacity now required.
- **pkg/vsjwt**: token validation now rejects non-RS256 algorithms (was accepting RS384/RS512); tokens without `exp` claim now rejected (was accepted indefinitely); `NewManagerWithPublicKey` returns `ErrNilPublicKey` for nil key (was `ErrNoPrivateKey`, misleading).
- **pkg/vtl**: `#foreach` and `#if` bodies now resolve `$input.*`, `$util.*`, and `$stageVariables.*` (were only running 4 of 8 pipeline phases — control flow, AppSync util, AppSync context, simple variables); JSON marshal failures during template processing now propagate as `Transform` errors (were silently producing empty strings).
- **Admin authentication**: admin API now rejects ID tokens (was accepting any validly-signed token regardless of `token_use`).

### Removed

- **IoT Events** — Service deleted (reached end-of-life on AWS). Handler, store, detector and alarm state machines, data-plane operations, HTTP routing, ARN builders, IAM managed policy actions, and SDK test utilities removed entirely. IoT rules `iotEvents` action type removed from the dispatcher; `WireActionCallbacksForRegion` simplified to drop the `batchPutFn` parameter, with rule dispatch callbacks now wired directly during IoT service initialisation.

## [0.0.12] - 2026-07-12

### Added

- **IoT Core** — Full MQTT broker with SHA-256 certificate-fingerprint authentication and IoT policy enforcement (Connect/Publish/Subscribe ACLs); thing registry, device shadows, certificate/CA/policy management, job execution fleet management, provisioning templates, topic rules engine (SQL with IN/NOT IN operators; 8 action types including DynamoDB/Kinesis/Lambda/SNS/SQS/Republish/IoTEvents/HTTP), per-region task engine with panic recovery, and CloudTrail audit stream integration.

- **IoT Events** — Detector model state machine (onInput/onEnter/onExit lifecycle, EventBus dispatch), alarm models, and data-plane operations (BatchPutMessage, Batch*Detector, Batch*Alarm) with IoTEventsData signing classifier.

- **DynamoDB Streams** — Stream ARN management, shard iterators, GetRecords with sequence-number pagination, atomic stream capture within item mutation transactions (PutItem, UpdateItem, DeleteItem, BatchWriteItem, TransactWriteItems, PartiQL), and DescribeStream with StreamViewType-aware image serialization.

- **RDS: embedded MySQL engine and Data API** — Isolated MySQL instances via go-mysql-server backed by Pebble KV storage (`RDS_MYSQL_ENABLED=true`); RDS Data API (ExecuteStatement, BatchExecute, Begin/Commit/Rollback Transaction) with named parameter substitution and 5-minute transaction TTL.

- **Neptune: TinkerPop Gremlin Server WebSocket** — Native WebSocket endpoint at `/gremlin` with GraphSON v3 serialization; per-cluster isolated graph engine with dedicated HTTP listener (REST + WebSocket); engines restored on restart.

- **S3: streaming chunked encryption** — `EncryptStream`/`DecryptChunked` process 64 MB chunks through AES-GCM without buffering entire body; per-chunk `PartEncryptionInfos` in SSE metadata.

- **Web console enhancements** — 3-panel S3 object browser (breadcrumb navigation, folder traversal, upload/download, batch delete, text/image preview); RDS instance management with engine lifecycle; PebbleDB key inspector; DynamoDB nested table→items navigation with structured attribute editor; CloudWatch alarm create dialog.

- **`ALL_SERVICES_ENABLED` environment variable** — Set to `true` to force-enable every service at once, overriding individual `*_ENABLED` flags.

- **API Gateway: validation and pagination** — List operations support `limit`/`position` pagination; `UpdateRestApi`, `CreateAuthorizer`, `CreateStage` validate input.

### Changed

- **CloudTrail audit config unified to `CLOUDTRAIL_ENABLED`** — Replaces `VS_AUDIT_ENABLED` and `features.audit_enabled`; now an Optional service (default `false`), propagated at startup.

- **Protobuf definitions fully regenerated** — All 34 service `.proto` files regenerated; webconsole TypeScript generation migrated from `protoc-gen-js` to `protobuf-es`; legacy `webconsole/src/gen/` directory removed (~270K lines deleted).

- **Web console: all service pages migrated to unified 3-panel inspector layout** — DataTable + Splitter + inspector components with global text filter, JSON/CSV export, and accessibility improvements (focus trap, `aria-modal`).

- **Neptune service packages relocated under `rds/`** — Neptune, NeptuneData, NeptuneGraph implementations and stores moved to `internal/{services,store}/aws/rds/`.

- **Dispatcher: removed resilience wrapper** — HTTP request dispatcher no longer wraps handler invocations in the resilience layer.

- **Internal refactoring across 20+ services** — Error mapping unified via `errors.Is` sentinel checks; store-level pagination (`common.List`, `ListMatching`, `ListMatchingProto`); response serialization consolidation; deterministic sorting for parameter lists and tags; dead code removal.

### Fixed

- **IAM**: `aws:*` condition key resolution (was comparing resolved value as key); explicit Deny precedence within a policy regardless of statement order; trust policy matching for Federated/Service/Account principals; `ResetPassword` lost-update race (added key lock); `GetAccountAuthorizationDetails` pagination beyond 1000.

- **S3**: PebbleDB key delimiter changed to null byte (`\x00`) eliminating collision risk with keys containing `#`; KeyLocker mutex race on persistent lock keys; multipart part sorting before assembly; versioned Range requests; `UploadPartCopy` Range propagation; `IsLatest` flag consistency on version delete; `CreateMultipartUpload` cleanup on failure; object lock operations I/O optimization; malformed XML/invalid part-number-marker error handling; `_latest` pointer lazy-write race eliminated.

- **DynamoDB**: Key attribute (partition/sort key) update rejection in `UpdateItem`/`TransactWriteItems`; item size calculation (AWS formula for Number/Map/List); stream OLD image capture for `OLD_IMAGE`/`NEW_AND_OLD_IMAGES` view types; atomic stream capture within transactions (was separate post-commit write with data-loss risk); backup scan error propagation; streams cleanup on `DeleteTableCascade` (now inside the transaction); pagination offset panic guard.

- **IoT**: Pagination offset out-of-range panic guard; task engine panic recovery (deferred closure fix); republish iteration counter type-assertion fix (was infinite loop); rule action map defensive copy (was overwriting persistent templates); shadow/`DeleteThing` unified lock domain (was concurrent, causing data loss); provisioning template body JSON validation on Create/Update/Proto conversion; billing group `ScanPrefix` error propagation; `ListShadowNames` pagination defaults and marker-not-found handling; `ioteventsdata` SigV4 signing classifier mapping.

- **Auth**: SigV4 bypass restricted to STS service only (was exploitable via form-encoded body); SigV4 RFC 3986 percent-encoding correction; `X-Forwarded-For` header ignored by default (security hardening).

- **EventBus**: Outbox writes now `pebble.Sync` for crash-safe at-least-once delivery; `UpdateStatus` serialized behind per-event mutex; `Publish` fail-fast on missing `EventRegistry`.

- **Misc**: Blob hybrid I/O error propagation (was silently truncating); `ProvisioningTemplate` proto conversion error propagation (`ProtoStoreConfig` interface updated to return errors); CloudWatch `ActionsEnabled` proto field promoted to `optional` for gRPC-Web parity; Cognito `SignUp` `AutoConfirmUser` inversion; Step Functions retry `MaxAttempts` off-by-one; Step Functions activity queue goroutine leak; Timestream query row double-wrap; zip-slip protection in `vstackscli` backup.

### Removed

- **Legacy webconsole TypeScript protobuf generation** — 37 files (~270K lines) from `webconsole/src/gen/` replaced by protobuf-es output.

## [0.0.11]

### ⚠️ Breaking Changes

- **Management console rewritten from Flutter/Dart to TypeScript/React**: Removed `web_ui/` (Flutter) and replaced with `webconsole/` (TypeScript, React, Vite, Tailwind CSS). The web console is now embedded into the Go binary via `//go:embed`. Build requires Node.js 22; `make build` runs proto generation and webconsole build automatically.
- **Configuration system overhauled**: Removed `ConfigSource` tracking (ENV/STORE/DEFAULT). Replaced `server.bind_addr` with `server.bind_mode` (all/localhost/interface) + `server.bind_interface`. Port defaults centralized in new `internal/common/serviceports/` package; environment variable reads removed from defaults layer.
- **Port allocation restructured**: Service endpoint ports defined as constants in new `serviceports` package. Added dynamic port allocator (`internal/server/portalloc/`) with FQDN/Individual port mode per service. Dynamic port range configurable via `VS_PORT_DYNAMIC_START`/`VS_PORT_DYNAMIC_END`.

### Added

- **TypeScript/React web console**: gRPC-Web admin interface with i18n (EN/JA/ZH), Tailwind CSS, JSON viewer, sidebar navigation, and settings management
- **Per-service enable/disable configuration**: 32 services individually controllable via `services.<name>.enabled` config keys and gRPC RPCs (EnableService/DisableService)
- **Root user authentication**: LoginRoot, InitialSetup, IsRootInitialized gRPC methods for initial setup flow
- **Server metrics RPC**: GetServerMetrics in AdminConfigService for runtime performance data
- **Protobuf service definitions**: Added proto files for ACM, AdminAuth, AdminConfig, APIGateway, AppSync, Athena, CloudFront, CloudTrail, and more in `proto/aws/`
- **Dynamic port allocation**: New `internal/server/portalloc/` package for resolving and binding service ports dynamically
- **Storage manager**: Centralized global storage initialization in `main.go`

### Changed

- **ACM overhauled**: Account configuration persisted to PebbleDB (was in-memory, lost on restart); certificate listing uses `common.List` with `ListByStatus` for server-side pagination replacing handler-level manual scan; tag operations migrated to `tagutil` framework removing `GetTags`/`AddTags`/`RemoveTags` store methods; `certificateToDetailResponse` omits zero-value `NotBefore`/`NotAfter` instead of fabricating timestamps; tag error mapping returns `ResourceNotFoundException`/`ValidationException` instead of raw errors; admin handler uses `GenerateCertificateId` replacing `time.Now().UnixNano()`; removed `GetByDomain`, `DeleteAccountConfiguration`, and wrapper functions
- **All AWS service admin handlers**: Unified constructor signature with StorageManager and AccountID parameters
- **API Gateway overhauled**: Patch operation parsing consolidated into `parsePatchOperations` helper, eliminating duplicated type-assertion boilerplate across all Update operations; deep-copy helpers unified via generic `deepCopyMap[T]`; unused `*ListResult` types removed; admin store uses `GetOrCreateStoreE` singleton pattern; fixed nil map serialization in `Update` (`ensureRestApiMaps`), binary media type remove with JSON Patch `-` (append marker), and missing `rootResourceId` in REST API responses; `UpdateApiKey` now invalidates runtime cache via `RemoveApiKey`
- **AppSync overhauled**: Response serialization consolidated into `response_mapper.go`, parse helpers into `helpers.go`; `extractTypeName` unified via `graphql.ExtractTypeName` shared utility; `GetApiById`/`GetGraphqlApiById` use `#id:` secondary index replacing full-table scans; fixed `UpdateApiById`/`UpdateGraphqlApiById` Put/Delete ordering (data loss on rename), `ServiceQuotaExceededException` HTTP 402→429; `DeleteDomainName`/`DeleteGraphqlApiById` cascade to domain associations; error mapping unified via `mapStoreError` across all operations; removed `jsonUnmarshal` alias, `sharedHTTPClientOnce`, `UpdateChannelNamespaceTags`
- **Athena overhauled**: HAVING/DDL result construction deduplicated (`evaluateWhere` reuse, `emptyDDLResult` helper, `buildResultSetFromStoredTable` delegation); `DropDatabase` cascades to tables and table data; `DeleteWorkGroup` cascades to named queries, prepared statements, query executions, and results; query execution store adds `#wg:` secondary index for workgroup-scoped operations, replacing full-table scans; cleanup changed from `sync.Once` to 24h periodic with cascade result deletion; removed `AthenaStoresInterface`/`AthenaStore` aggregate, `ListQueryExecutionsWithPagination`, `GetDefaultWorkGroup`
- **CloudFront overhauled**: `UpdateDistribution` fixed WAF WebACL change detection — old config was overwritten before comparison so WAF association/disassociation never fired; `ListInvalidations` removed duplicate top-level fields outside `InvalidationList`; tag parsing deduplicated into `parseXMLTags` shared by `CreateDistributionWithTags` and `TagResource` (replacing 2 inline copies); `newCloudFrontError` wrapper removed in favour of direct `awserrors.NewAWSError` (10 sites); `generateETag` service method removed (already handled inside `UpdateWithLastModified`); `TrustedSigners` no-op assignment removed; `UpdateCachePolicy` now parses `ParametersInCacheKeyAndForwardedToOrigin` matching `CreateCachePolicy`; store `Create` methods on 4 policy stores add mutex; `AdminHandler` caches `DistributionStore` instead of recreating per call; `DistributionServer.getDistributionStore` returns cached store on error instead of nil store
- **CloudTrail overhauled**: Trail resolution consolidated into `ResolveTrail` replacing scattered `GetTrail`/`GetTrailByARN` conditionals; `resolveBool`/`applyTrailUpdates` unify bool parsing and field assignment for `CreateTrail`/`UpdateTrail`; `buildIndexKeys`/`applyIndexKeys` deduplicate index operations across add/remove/txn paths; `eventMatchesQuery` delegates to `protoMatchesQuery`; `listAllTrails` replaces `MaxItems:10000` with paginated iteration; `ListTrails` uses store-level Marker pagination; `GetEventByID` unified to always read proto; `EventCategory` added to Event proto message
- **CloudWatch alarm operations refactored**: Create-or-update logic deduplicated into `upsertAlarm` shared by `PutMetricAlarm`/`PutCompositeAlarm`; tag parsing extracted to `parseAlarmTags`; alarm type resolution and action history logging extracted to `resolveAlarmType`/`addAlarmActionHistory`; `alarmToResponse` uses `alarm.CreatedAt` as fallback replacing non-deterministic `time.Now()`; removed dead CWL types from cloudwatch store package
- **CloudWatch Logs overhauled**: subscription and metric filter evaluation deduplicated via `evaluateMetricFilters`/`deliverSubscriptionEvents` replacing duplicated HTTP and bus handler paths; destination persistence migrated from JSON to protobuf with dedicated proto message; `DeleteLogGroup` uses paginated stream deletion (was limited to 1000); `LogGroupClass` added to proto round-trip (was lost on persistence); admin `CreateLogGroup` uses `NewLogGroup` constructor setting `Region`/`AccountID`; `PutLogEvents` rejects empty events; `log_writer` tolerates `ErrLogStreamAlreadyExists` on auto-create; `ErrLimitExceeded` store error mapping added
- **Cognito Identity overhauled**: Login/provider map parsing consolidated into generic `parseMapParam`, removing `parseLogins` and `parseSupportedLoginProviders`; map-key extraction deduplicated via `formatLoginKeys`; fixed `GetOpenIdToken` variable shadow causing redundant store fetch; `DeleteIdentityPool` now cascades to developer identities and principal tag attribute maps; `NewIdentityPool` timestamps deferred to store layer; removed unused `generateIdentityID`
- **Cognito IDP overhauled**: Authentication flows (`USER_PASSWORD_AUTH`, `ADMIN_NO_SRP_AUTH`, refresh token) deduplicated into `authenticateUser`/`refreshAuthToken`/`respondToNewPasswordChallenge` shared methods; `findTokenByValue[T]` generic helper replaces three identical `GetTokenByValue` implementations; `tokenKey` unifies `refreshTokenKey`/`idTokenKey`/`accessTokenKey`; `applyUserPoolUpdates` consolidates `CreateUserPool`/`UpdateUserPool` field assignment; `parseIntParam` eliminates `getIntParam`/`getIntParamOK` duplication; `parseUserPoolAddOns`/`parseAccountRecoverySetting`/`parseUsernameConfiguration`/`parseDeviceConfiguration` parsers added; `DeleteUserPool` now cascades to users, groups, clients, tokens, and challenge sessions; pagination added to `ListGroups`, `ListUsers`, `ListUsersInGroup`, `AdminListGroupsForUser`, `ListUserPoolClients`; `SignUp` generates and persists confirmation code; `ConfirmSignUp` validates code with constant-time comparison and expiry check; hosted UI hardened against XSS via `html.EscapeString`; `handleTokenEndpoint` returns `expired_grant` for expired refresh tokens
- **DynamoDB overhauled**: GSI/LSI query logic deduplicated via `queryByIndex`; key condition extraction consolidated into `extractKeyConditionFromSchema`; sort logic unified with `sortItemsBySortKeyWithIndexDirection`; `DeleteTableCascade` now removes backups, exports, imports, and global table entries; transaction validation uses `NewTxn` interface method instead of type assertion; `ItemStore.Put` no longer mutates input attributes map; fixed `BatchWriteItem` table-not-found error handling, `CreateTable` TableClass persistence, backup duplicate name prevention, `DeleteAllForTable` error handling, and unparseable key handling in `BatchGetItem`; deterministic ordering for attribute definitions and GSI updates; scan operations now surface unmarshal errors
- **EC2 overhauled**: 4 Authorize/Revoke Ingress/Egress operations deduplicated into `modifySecurityGroupRules` helper with closure pattern; `ipRuleEquals` extends duplicate/revoke matching from protocol+port-only to full IpRanges/Ipv6Ranges/UserIdGroupPairs comparison; `parseIPRules` parses GroupId/GroupName/UserId/VpcId/Ipv6Ranges/Description (was GroupId+CidrIp only); `DescribeVpcs`/`DescribeSubnets`/`DescribeSecurityGroups` gain EC2 Filter.N support (`parseFilters` + typed match helpers); `DeleteVpc` checks for dependent subnets and security groups (`DependencyViolation`); `CreateSecurityGroup` rejects duplicate GroupName within VPC (`InvalidGroup.Duplicate`); `storeForRegion` extracts store lookup removing 4 duplicated paths; `ec2store.Tag` replaced with shared `types.Tag`; `getStringParam`→`request.GetStringParam`, `ParseTags`→`tags.ParseTagsWithPrefix`, `parseInt64`→`strconv.ParseInt`
- **EventBridge overhauled**: `eventBusToMap`/`ruleToMap`/`archiveToMap` consolidate response serialization; `mapStoreError` unifies ~20 store error checks; `resolveTaggableResource` supports 5 taggable resource types; `GetStoreForRegion` extracts store caching; Connection/API Destination split into dedicated files; `DeleteEventBus` cascades to rules→targets→tags→archives→events; `DeleteArchive` cascades to events; `ListRuleNamesByTarget` rewritten with full-scan + client-side pagination; fixed `RemoveTargets` missing rule check, `ListApiDestinations` duplicate fields; added duplicate-name guards on Create and in-use guard on `DeleteConnection`; `DescribeEventBus`/`DescribeRule` now return Tags; `strings.HasPrefix` replaces manual prefix matching; removed `getEventBusByArn`/`getRuleByArn`, admin `storageManager`
- **IAM service refactored**: Global IAM store singleton (`GetOrCreateGlobalStore`) prevents redundant policy seeding; attached and inline policy operations deduplicated via ops-struct pattern; store hoisting in instance profile responses; fixed `GetAccountAuthorizationDetails` pagination (was truncated at 1000) and `GetAccountSummary` ServerCertificates count (was hardcoded 0); credential report access keys now sorted deterministically
- **Kinesis overhauled**: Stream name/ARN resolution consolidated into `resolveStreamName` (7 operations); `PutRecord` shard selection and write atomic via `PutRecordWithShardSelection` (TOCTOU fix); `GetRecords` returns actual EncryptionType instead of hardcoded "NONE"; `ListStreams` uses store-level pagination replacing full-scan; `initShardIDCounter` prevents shard ID collisions on restart; `cleanExpiredIterators` throttled to 5-minute intervals; consumer/format helpers extracted; error mapping unified via `mapStoreError`
- **KMS overhauled**: UUID v4 key ID generation on `CreateKey` (was empty string); `DeleteAlias` authorization check; `Sign`/`Verify` algorithm validation against key's supported algorithms; key pair generation moved to HSM layer with `GenerateKeyPair` Backend method; `resolveKeyByKeyID` delegates to `resolveKey`; `GetPublicKey` reads algorithm lists from persisted key struct; grant response building deduplicated; fixed `ListRetirableGrants` nil-entry slice; `KeyUsageEncryptDecrypt` excludes ECC key specs (AWS spec compliance); `buildKeyMetadata` uses persisted `MultiRegionConfiguration`
- **Lambda overhauled**: Function and version configuration responses deduplicated via `configFields`/`buildConfigMap`; CORS config parsing consolidated with `applyCorsFields`; deprecated `UpdateAlias` and `AddPolicy` store methods removed in favor of atomic variants; fixed async invocations (`Invoke`/`InvokeAsync`) not passing resolved version, `SetProvisionedConcurrency` using hardcoded account ID, and host endpoint port formatting; `AddPermission` now rejects duplicate StatementId; `DeadLetterConfig` validates TargetArn, `TracingConfig` validates Mode
- **Neptune ecosystem overhauled**: Neptune events migrated to `ProtoStore[Event]`; `clusterToResponseMap` replaces JSON round-trip; parameter group handlers deduplicated; `OpenClusterEngine`/`clusterBucket` accept explicit region parameter. NeptuneData Gremlin/OpenCypher query status/list/cancel deduplicated into shared methods; admin gRPC handlers consolidated; loader cancellation refactored to per-job channels enabling concurrent independent cancellation; `nodeIDMap` instance field removed for per-load local maps; `traversalToSteps` shared between explain and nested traversal; ntriples loader checks cancel in scan loop. NeptuneGraph bulk import batching consolidated into `importBatcher` struct; periodic expired-task cleanup via `regionCleanups` sync.Map with 24h production interval (5m/30m in test mode); dead `isCreate` parameter, `parquetTypeToPb` branch, `sortedPropKeys` removed; `endpointToResponse` includes `graphId`; `resolveGraphIdentifier` deduplicates header lookup; `s.region`→`reqCtx.GetRegion()`; proto3 zero-value fields mapped to nil
- **Route 53 overhauled**: Health check and delegation set response construction deduplicated into `healthCheckToResponse` (4 sites) and `buildDelegationSetResponse` (3 sites); tag parameter parsing extracted to `parseResourceParams` removing duplicated validation; admin `CreateHostedZone` auto-creates NS/SOA records, converts proto VPC region enum via `protoVPCRegionToAWS`, sets `Region`, and always initializes `Config`; `ListResourceRecordSets` defaults `maxItems` to 300; `normalizeZoneNameForCreate` migrated to exported `NormalizeZoneName`; fixed `ChangeTagsForResource` double-tagging bug; removed unused store types and helpers
- **S3 service overhauled**: Store acquisition hoisted to handler layer; EventBridge notification support; SNS/SQS `MessageAttributes`; copy operations preserve source StorageClass; `CompleteMultipartUpload` ETag validation; fixed `ListObjectVersions` pagination, `ListMultipartUploads` key parsing, website routing rules, and chunked encoding bounds
- **Scheduler overhauled**: Target parsing deduplicated via `coerceToMap`/`parseTargetFromMap` replacing duplicated string/map branches; case-insensitive field access unified with variadic-key helpers (`getMapField`/`getFloatField`/`getBoolField`/`getSliceField`); AWS cron field conversion consolidated into `awsCronReplacements` map loop; empty-input default unified across all target types into `scheduleInput`; `getScheduleNameAndGroup`/`getListGroupName` extract duplicated parameter parsing; `getStoreForSchedule` extracts engine store lookup with cache-first optimization; store prefix checks simplified with `strings.HasPrefix`; `handleBusDelivery` adds engine nil guard
- **SESv2 overhauled**: 13 Put/Create/Update operations deduplicated via `updateConfigSet`/`updateEmailIdentity`/`putEmailIdentityPolicy` helpers with modifier closures; `generateDkimToken` switched from deterministic `time.Now().UnixNano()` to `crypto/rand`; `parseStoredTime` fallback changed from non-deterministic `time.Now()` to `0`; `ListEmailIdentities` derives `VerificationStatus` from DKIM/VerifiedForSending (was hardcoded `"SUCCESS"`); `PutEmailIdentityDkimSigningAttributes` now reads `SigningAttributesOrigin` (was no-op); `GetContactList` response includes `DisplayName`/`Description` on topics; duplicate `SuppressionReason` field removed; store sentinel errors propagated directly replacing `awserrors` wraps; dead `ContactListStore`/`ContactStore` types removed; AdminHandler caches store per region via `sync.Map`
- **Secrets Manager overhauled**: ARN→name secondary index (`#arn:` prefix) replaces full-table scans; `UpdateSecretVersionStage` stage rotation atomic via store-level `MoveStage` (race condition fix); `DeleteSecret`/`GetSecretVersionByStage` use `secret.VersionIDs` replacing full-table scans; password generation uses Fisher-Yates shuffle (was deterministic fixed positions); fixed `UpdateSecret` SecretString/SecretBinary overwrite, operator precedence in replication condition; `applySecretFilters` deduplicated via `filterSecrets`/`matchesAny`; `List`/`ListProto` skip `#`-prefixed index keys
- **SNS overhauled**: Message attribute parsing consolidated into `parseMessageAttributes` with `firstString` case-insensitive key lookup and Binary attribute base64 decode (was dead `[]byte` cast); `messageAttributeValue` helper ensures Binary values are base64-encoded in SQS payload, HTTP notification, and Lambda event envelope; SNS→SQS delivery uses typed `SQSMessageAttribute` (`DataType`/`BinaryValue`/`StringValue`) preserving attribute types through the event bus; `Subject` and `MessageAttributes` added to `SNSDeliveryEvent` across Publish, PublishBatch, and PublishToTopic; subscription list construction deduplicated into `buildSubscriptionList`; permission injection extracted to `injectPermissionsIntoPolicy`; `parseAttributes` case-insensitive key search consolidated into loop; endpoint attribute merge extracted to `mergeEndpointAttrs`; `buildNotificationPayload` unifies Lambda/HTTP delivery; `ErrTopicNotFound` package-level variable replaces `NewTopicNotFoundException`; unused `TopicListResult`/`SubscriptionListResult` removed; `GetTopicAttributes` returns `KmsMasterKeyId`, `EffectiveDeliveryPolicy`, `CreatedDate`, `LastModifiedTime`; `SetTopicAttributes` syncs attributes to struct; fixed `ListEndpointsByPlatformApplication` marker inclusion and signature string composition
- **SQS overhauled**: FIFO ReceiveMessage with per-MessageGroup in-flight isolation; `SQSInvoker.SendMessage` signature changed to `SQSSendOptions` struct; `SetQueueAttributes` expanded to `FifoQueue`, `ContentBasedDeduplication`, `RedrivePolicy`; `ChangeMessageVisibility` and DLQ move now use atomic transactions
- **SSM overhauled**: List parameter parsing consolidated into `parseStringList` helper (5 call sites); `GetParametersByPath` path normalized with trailing `/` suffix (fixes `/foobar` false-positive prefix match); admin `PutParameter` supports `KeyID`, `Tier`, and `Tags`; `DescribeParameters` Type/Tier hardcoded defaults removed in favor of existing switch mapping; `DeleteParameter` history deletion paginated (MaxItems 1000 loop replacing single 10000 batch); `DescribeParameters` gains `DataType` filter; `GetParametersByPath` non-recursive path matching simplified; fixed `PutParameter` tag guard dropping tags on overwrite
- **Step Functions overhauled**: `DeleteStateMachine` cascades to executions→history events, versions, and aliases via `ForEach` full-scan with collect-then-delete pattern; `DeleteActivity` cascades to pending tasks with channel close; activity queue drain now closes channel to prevent goroutine leaks; `deleteExecutionHistory` extracted as helper; fixed `StartSyncExecution` removing incorrect EXPRESS rejection (was backwards—EXPRESS is the type that supports sync); `ListMapRuns` omits empty `nextToken`; `extractStateMachineName` replaced with `arnutil.ExtractStateMachineNameFromARN` for proper ARN parsing; `getStringOrEmpty` deduplicated to `getStr`; `isErrNotFound` uses `errors.Is` sentinel errors
- **STS and admin_auth overhauled**: `resolveRoleForAssume` deduplicates role resolution across AssumeRole/AssumeRoleWithSAML/AssumeRoleWithWebIdentity; ARN format fixes in `GetCallerIdentity` (SAML, FederatedUser, WebIdentity); SAML assertion and WebIdentity token validation; `GetDelegatedAccessToken` uses delegated token store with single-use redemption; admin_auth JWT generation uses `vsjwt.JWTUser` directly, removing type-switch duplication
- **Timestream Query and Write overhauled**: Scheduled query response formatting deduplicated into `formatScheduledQueryBaseResponse` with list/description overrides replacing two independent builders; `ScheduledQueryState` type consolidated into `ScheduledQueryStatus` (same values); `UpdateScheduledQuery` store switch matches only valid states (was silently treating invalid as DISABLED); `GetQueryStatus`/`ListQueryResults` dead methods and `CachedRows` redundant field removed; `ErrInvalidParameter` merged into `ErrValidationException` (13 call sites); `mapStoreError` uses `errors.Is` sentinel errors replacing string comparison; `ListTables` validates empty database name; store `writeChunkBatch` returns `[]error` for partial version conflict reporting; `BatchLoadTaskStore` unused `TagStore` embed, `TimestreamStoresInterface`/`TimestreamStores` aggregate, and `NewRecordStore` non-indexed variant removed
- **WAFv2 overhauled**: Resource summary builders deduplicated into `buildWebACLSummary`/`buildRuleGroupSummary`/`buildIPSetSummary`/`buildRegexPatternSetSummary` (10 sites); IP address and regex list parsing extracted to `parseAddressList`/`parseRegularExpressionList`; `convertAction` handles lowercase JSON-deserialized keys; `convertActionToResponse` supports Captcha and Challenge actions; `UpdateRuleGroup` supports partial updates; `PutLoggingConfiguration` preserves `RedactedFields` as pass-through; `ListLoggingConfigurations` uses pagination helpers; `LoggingStore.Create` adds mutex; `Get` replaced by `GetByResourceArn`; removed unused store types and fields
- **CI/CD**: Added Node.js 22 setup, webconsole dependency install, and proto generation steps
- **Cypher parser refactored**: Edge target resolution deduplicated into `resolveEdgeTarget` (replacing 8 inline blocks across 6 files); result modifier logic (ORDER BY/SKIP/LIMIT/DISTINCT) extracted to `applyResultModifiers` (5 call sites); property resolution consolidated from 5 near-identical functions into `resolvePropsMap`/`resolvePropValue` with thin wrappers; `crossProduct` renamed to `crossProductBindings`; fixed MATCH WHERE silently ignored when WITH clause present (`matchWhere`/`matchVarLengthBindings` removed incorrect `q.With` guards); dead `evalRowExpr` wrapper and `OpNeq` unreachable branch in `evalComparison` removed; edge property check moved before row allocation in optional match
- **Gremlin parser refactored**: Edge traversal step execution deduplicated into `execEdgeTraversal` (replaces identical `execOutE`/`execInE`/`execBothE` bodies); `execFrom`/`execTo` merged into `execFromTo` with `tagKey` parameter; `execGroup`/`execGroupCount` share `groupMods` and `groupBy` generic grouping helper; `execProperty` Node/Edge update paths consolidated via `elementIDForUpdate`/`resolvePropertyValue`; `execElementMap` map construction extracted to `buildElementMap`; `isSlice` simplified from `reflect` to `[]any` type assertion; `resolveArgValue` non-string map key handling changed from returning error value to `continue` (was unreachable error path); fixed `property()` step `set` cardinality writing redundant updates when value already present (idempotency restored via `valuesEqual` guard)
- **Dependencies**: Added gopsutil (indirect: purego, go-ole, plan9stats, perfstat, go-sysconf, numcpus, wmi)
- **Documentation**: Updated README (EN/JA/ZH) with GUI screenshot and Docker requirements
- **Makefile**: Build now generates proto TypeScript before webconsole build; binary name updated

### Removed

- **Flutter/Dart web UI** (`web_ui/` directory deleted)
- `internal/common/defaults/` package (superseded by `serviceports`)
- `ConfigSource` type and `Source` field from config entries
- `getEnvString`, `getEnvInt`, `getEnvBool` helper functions from config defaults

## [0.0.10] - 2026-04-27

### Added
- **Temporary session credentials support** (`internal/server/http/auth_middleware.go`): AWS Signature middleware verifies both static and temporary (STS) credentials via `SessionResolver`
- **S3Invoker for EventBus** (`internal/eventbus/service_invokers.go`): cross-service S3 object operations enabling DynamoDB export/import to S3
- **DynamoDB Step Functions integration**: `GetItem`, `PutItem`, `DeleteItem`, `UpdateItem` task support in Step Functions service
- **CloudFront ListKeyGroups operation** and Location header handling in responses
- **IAM MFADevice name requirement** and server certificate rename support
- **Resource tracking in audit events**: `ResourceTypes` field and `buildResources` function for CloudTrail event enrichment
- **IAMPrincipalResolver**: access key ID to username resolution for audit logging
- **Defaults package** (`internal/common/defaults/`): centralized `DefaultRegion` constant replacing scattered references
- **SDK tests massively expanded** (290 files, +66,464/-44,016 lines): full lifecycle coverage for ACM, API Gateway (14 test files), AppSync (13 files), Athena (8 files), CloudWatch/Logs, Cognito, DynamoDB, EventBridge, KMS, Neptune, S3, SESv2, SQS, WAFv2 (5 files), and more — now 2262+ tests total

### Changed
- **awscli full-service validation pass**: comprehensive awscli-driven testing across all services uncovering and fixing integration issues
- **DynamoDB import/export refactored** (`import_export_operations.go`, 260 lines): S3-backed export/import with improved error handling and validation
- **KMS key operations refactored**: `CreateKey` handles external-origin keys (PendingImport state), `ImportKeyMaterial` validates tokens and decrypts wrapped keys
- **SESv2 configuration handling enhanced**: VDM attributes, contact list topic handling, configuration set event destination management
- **API Gateway service hardened**: conflict detection in `CreateResource`, authorization type validation in `PutMethod`, `CreateRestApi` Policy parameter, cascading `UpdateResource`, streamlined `TestInvokeMethod`
- **Cognito user pool configuration expanded**: email/SMS configuration parsing, new user pool attributes and settings
- **AppSync introspection and associations fixed**: `ListTypesByAssociation` validation, `ListSourceApiAssociations` merged API references, `StartDataSourceIntrospection` consistent failure response
- **Athena service hardened**: resource-not-found errors for catalog/workgroup/prepared statement queries, pagination in `ListDataCatalogs`/`ListPreparedStatements`, SELECT without FROM support
- **CloudWatch metric response formatting** and log group operations improved
- **State machine definition JSON validation** added, duplicate creation error handling
- **Audit event builder enhanced**: S3 event determination logic, event source mappings, event filtering by `EventID`/`ReadOnly`/`EventSource`/`AccessKeyID`
- **SDK test utilities refactored**: monolithic test files split into focused modules (e.g., `apigateway.go` → 14 files, `appsync.go` → 13 files, `athena.go` → 8 files)

### Fixed
- S3 encryption handling
- DynamoDB condition expression handling
- API Gateway error responses with specific not-found exceptions
- SQS policy generation in `GetQueueAttributes`
- SSM parameter label management (`UnlabelParameterVersion`), validation error handling, `PutParameter` tags silently dropped on overwrite, `GetParametersByPath` prefix false-positive match
- WAF duplicate resource error messages
 - OIDC provider ARN construction for URL prefixes
- SESv2 response structure inconsistencies
- Region handling edge cases across services

## [0.0.9] - 2026-04-26

### Added
- **EC2 resource stubs** (new): VPC, Subnet, Security Group data models and store layer for cross-service resource references (`internal/services/aws/ec2/`, `internal/store/aws/ec2/`) — not a functional compute service
- **vstacks CLI** (`cmd/vstacks/`): management tool for server control, IAM, config, service, and backup operations
- **PebbleDB batch operations**: `CommitSync` method for atomic writes; batch interface (`internal/core/storage/batch.go`, `pebble_batch.go`)
- **Goroutine pool** (`internal/core/resilience/goroutine.go`): bounded goroutine execution for resilience
- Unit tests: AppSync schema operations & WebSocket server, DynamoDB item condition/expression/key condition/table updater, Neptune pagination & global/event operations, store tag tests (362 lines)

### Changed
- **Neptune/NeptuneGraph enhanced**: graph engine moved from `pkg/graphengine/` to `internal/core/storage/graphengine/` with expanded KV-backend store (845+ lines tests); service handler split (`service.go` → export/import/queries/snapshots/tags/responses); admin operations refactored (cluster, subnet group, error handling); Cypher parser DDL support (CREATE/DROP index) and modularization into ~10 files; Gremlin parser modularized into 5 step files + helpers; N-Triples serialization added (`internal/utils/ntriples/`); EventBus NeptuneGraph invoker; NeptuneGraph parser and error handling improved
- **Request parsing refactored** (`internal/common/request/`): monolithic parser simplified via registry pattern; per-service parsers added; `interfaces.go` removed
- **Tag handling refactored** (`internal/common/tags/`): operations extracted into dedicated handler with `UntagResource` by keys; removed `internal/common/types/tag.go`
- **Service handler file splits**: API Gateway integration (`aws.go` → `aws_dynamodb.go` + `aws_sns.go` + `mapping.go`), S3 (`handler_bucket.go` → put/get/delete/response)
- **Lambda ESM poller and tag operations refactored**: improved stream polling and streamlined tag management
- **AppSync refactored**: evaluation operations expanded (+507 lines), datasource simplified (−300 lines), tag operations enhanced (+180 lines)
- **EventBus concurrency handling enhanced** (`bus.go`, `invoker.go`)
- **PebbleDB improvements**: TTL handling, transaction management, iterator, and lock mechanisms
- **Blob storage refactored** (`blob_hybrid.go`)
- **Docker (Moby) client refactored** (`internal/client/mobyclient/`): container, network, and volume operations with dedicated error types (`errors.go`)
- **Server wiring refactored** (`internal/server/apps/`): adapters, optional services, and service initialization streamlined
- **Resource extractor refactored** (`internal/server/authorization/resource_extractor.go`)
- Pagination helpers cleaned up, XML encoding improved, region handling simplified
- **SDK tests enhanced**: expanded coverage for SNS, SQS, Secrets Manager, Step Functions (+1661/-527 lines)

### Fixed
- Adaptive timeout redundant return statement (`internal/core/resilience/adaptive_timeout.go`)
- Error handling and unused code cleanup across multiple services (AppSync, NeptuneGraph, KMS, S3, SESv2, Route53, WAF, etc.)
- Lambda redundant response header handling

### Removed
- **Utility package consolidation** (~4270 lines): deleted `internal/utils/archiver/`, `internal/utils/naming/`, `internal/utils/netutils/`, `internal/utils/timeutils/`, `internal/utils/aws/conditions/`, `internal/utils/aws/helpers/`, `internal/utils/aws/parsers/`, `internal/utils/crypto/` (consolidated into core packages)
- Deleted `internal/common/types/tag.go`, `internal/common/request/interfaces.go`, `internal/store/aws/common/types.go` (absorbed into other packages)
- Deleted service-specific operation files absorbed by refactoring: Cognito user operations, Lambda function operations, Secrets Manager secret operations

### Documentation
- Updated README (English, Japanese, Chinese) with latency metrics and standardized `DATA_PATH`
- Updated configuration reference (`docs/configuration.md`)
- Updated architecture and services documentation
- Updated LocalStack comparison report

## [0.0.8] - 2026-04-09

### Added
- Neptune Graph (Neptune Analytics) service with graph store, RDF/SPARQL support, vector embeddings, topK search, and query procedures (`neptunegraph/`, 2265+ lines)
- Neptune Graph SDK tests (895 tests) with host prefix middleware workaround
- Graph engine vector embedding storage with in-memory cache, cosine/Euclidean/inner-product distance functions, and brute-force topK search (`pkg/graphengine/vector.go`, 771 lines)
- Lambda AWS Event Stream binary encoding support (`lambda/eventstream.go`) for streaming invoke responses
- Test helper scripts: `run_tests.sh`, `setup_test_credentials.sh`, `cleanup_test_resources.sh`, `test_authorization.sh`
- NeptuneGraph request parser (`internal/common/request/neptunegraph_parser.go`)
- App wiring layer (`internal/server/apps/`) for modular service initialization and dependency injection
- Authorization module extracted from dispatcher (`internal/server/authorization/`) with enhanced resource extraction for NeptuneGraph
- `internal/common/request/context.go` as shared request context (replaces per-service copy)
- Unit tests for endpoint builder, error factories, handler registrar, tags operations, resilience (adaptive timeout, bulkhead, circuit breaker, retry, cache, health, metrics), PebbleDB, auth credentials, eventstream encoder, and validators

### Changed
- Common packages moved from `internal/services/aws/common/` to `internal/common/` (auth, endpoint, errors, iam, kms, lambda, mock, pagination, protocol, region, request, response, tags, types, audit)
- Event bus moved from `internal/server/eventbus/` to `internal/eventbus/`
- All 26+ service implementations updated to import from new `internal/common/` paths
- `main.go` restructured: service initialization delegated to apps wiring layer (675-line refactor)
- All services now receive dependencies via setter injection (removing direct storage manager coupling)
- DynamoDB error constants expanded with comprehensive godoc documentation; removed unused expression files (`item_condition.go`, `item_expression.go`, `item_sort.go`, `partiql_expression.go`, `partiql_value_parser.go`, `input_output.go`)
- Neptune service expanded: cluster, instance, snapshot, parameter group, and subnet group operations with pagination support
- Neptune Data service enhanced with query status tracking, statistics management, and improved error handling
- S3 SSE-S3 encryption support, enhanced access control, and updated chunked upload handling
- Lambda invoke operations enhanced with response streaming and event stream support
- Cypher parser extended with CALL statement support and pipeline execution
- Gremlin parser enhanced with additional filter and source step operations
- AppSync GraphQL datasource simplified, WebSocket server improved
- CloudFront distribution server and policy operations enhanced
- CloudWatch alarm evaluator multi-region support with configurable tick intervals
- Scheduler engine enhanced with Step Functions target and configurable intervals
- Step Functions enhanced with JSONata evaluation, Map state processor, and redrive operations
- EventBridge refactored to multi-region store model with improved archive and replay operations
- PebbleDB transaction handling, iterator, TTL, and bucket operations improved
- Graph engine store and traversal implementations optimized
- Filter pattern evaluator and parser improved
- SDK tests updated for CloudWatch, Cognito, Lambda, NeptuneData, SESv2, SNS, SQS, StepFunctions, STS, Timestream, WAFv2; added NeptuneGraph test registration
- Updated documentation: architecture, configuration, services, Terraform guide, LocalStack comparison, README files
- Dependabot: bumped AWS SDK v2 dependencies (lambda, cloudwatchlogs, kinesis, s3, eventstream, OpenTelemetry)

### Fixed
- CloudTrail recorder import path corrected after audit package relocation
- SNS publish ARN resolution for event bus delivery
- Various store interface and type consistency fixes across services

## [0.0.7] - 2026-04-05

### Added
- AppSync service (GraphQL API v1, Event API v2) with full CRUD for APIs, data sources, resolvers, functions, types, schemas, API keys, caches, domain names, and merged APIs
- AppSync GraphQL engine with VTL resolver execution, introspection, pipeline resolvers, and `$ctx` context variables (`$ctx.args`, `$ctx.source`, `$ctx.identity`, `$ctx.info`, `$ctx.stash`, etc.)
- AppSync WebSocket server for real-time subscriptions and mutations
- AppSync request parser supporting both v1 (GraphQL) and v2 (Event API) protocols
- VTL engine AppSync context extensions (`pkg/vtl/appsync_context.go`, `appsync_util.go`)
- WAF gRPC-Web proto definitions and generated code
- Gremlin parser enhancements: comprehensive AST, lexer improvements, new filter steps (`where`, `has`, `select`, `order`, `group`, `dedup`, `path`), and source steps (`V`, `E`, `addV`, `addE`)
- Event bus resource extractors for AppSync and Neptune (dispatcher audit logging)
- Integration test suite (1748 lines) covering cross-service event flows: EventBridge→Lambda/SQS/SNS/Kinesis/StepFunctions, ESM SQS/Kinesis→Lambda, CloudWatch Alarm→SNS/Lambda/StepFunctions, Scheduler→Lambda/SQS/SNS/StepFunctions, SFN Task→Lambda/SQS/SNS, S3 Notification→Lambda
- SDK test service registry with category support (SDK, WebSocket, Integration)
- SDK tests for AppSync (2135 lines) and AppSync WebSocket (740 lines)
- Additional SDK test coverage for DynamoDB (backup), KMS, Lambda, and other services

### Changed
- Proto packages renamed to match AWS service naming conventions (`rds→neptune`, `email→sesv2`, `states→sfn`, `monitoring→cloudwatch`, `events→cloudwatchevents`, `logs→cloudwatchlogs`, etc.)
- Neptune admin handler expanded with full DescribeDBClusters/Instances and gRPC-Web management operations; proto migrated from RDS package
- Step Functions now subscribes to event bus for cross-service start execution events (EventBridge, Scheduler, CloudWatch Alarms); added Map state `ItemProcessor` and enhanced parallel execution
- EventBridge refactored to multi-region store support (`sync.Map` per-region stores); added Kinesis stream target delivery
- Lambda ESM poller enhanced with Kinesis stream source support (shard iterator, checkpoint tracking)
- CloudWatch alarm evaluator extended to multi-region evaluation with TEST_MODE 1-second tick interval
- EventBridge Scheduler engine enhanced with Step Functions target support and TEST_MODE 1-second ticker
- SNS/SQS admin handlers refactored to delegate to shared service instances instead of owning storage
- SNS publish operations now resolve SNS topic ARN components for event bus delivery
- AppSync ARN builder added to `internal/utils/aws/arn/builder_services.go`

### Documentation
- Updated README files (English, Japanese, Chinese) with AppSync service and updated test counts
- Updated SDK test README with integration test documentation
- Updated ACM proto with expanded field coverage

## [0.0.6] - 2026-04-04

### Added
- Event bus system (`internal/server/eventbus/`) with Pebble outbox store, IAM role resolver, policy evaluator, and subscription management for cross-service event routing
- HTTP request classifier (`internal/server/http/classifier/`) replacing legacy ServiceRouter — classifies requests by protocol (REST-XML, REST-JSON, AWS JSON, Query, CBOR) and service
- CloudWatch alarm evaluator engine extracted from admin handler (`alarm_evaluator.go`, 629 lines)
- Lambda Event Source Mapping (ESM) poller for DynamoDB/Kinesis streams (`esm_poller.go`)
- Secrets Manager rotation engine with configurable rotation strategies (`rotation_engine.go`)
- Cognito User Pool trigger pipeline (Pre/Post Sign-up, Sign-in, MFA, Token, Auth challenges — `triggers.go`)
- S3 event notifications (ObjectCreated, ObjectRemoved, ObjectRestore) published via event bus (`notifications.go`)
- CloudWatch Logs subscription filter delivery via event bus (`bus_handlers.go`, `log_writer.go`)
- Neptune Data API: Gremlin, OpenCypher, loader, statistics, and explain handlers refactored into dedicated files
- PebbleDB iterator extracted into `db_iter.go`, lazy iterator (`db_lazy_iterator.go`), and TTL support (`db_ttl.go`)
- Blob storage split into `blob_multipart.go`, `blob_reader.go`, `blob_versioning.go`
- AWS SigV4 credential parser utility (`internal/utils/aws/authutil/credential.go`)
- Common request helpers: `GetBoolParam`, `GetIntParam` in `internal/services/aws/common/request/`

### Changed
- Dispatcher refactored: extracted `executeHandler` pipeline, added `DispatchClassified` for classifier-based routing
- Admin handlers across 20+ services refactored: gRPC stubs removed from Neptune Data, Kinesis, SNS, SQS, EventBridge; large monolithic handlers trimmed
- gRPC-Web error constructors renamed from `ErrXxx()` to `NewXxxError()` for consistency
- NeptuneData error type simplified — removed wrapper struct, returns `*awserrors.AWSError` directly
- Service index builder extracted (`router/service_index.go`)
- Removed legacy files: `request_extraction.go`, `router/service_router.go`, `router/path_patterns.go`, `core/events/bus.go`
- Removed docs: `docs/integration.md`, `docs/new-service-guide.md`
- IAM role resolver wired into event bus at startup for trust policy evaluation

### Documentation
- Updated architecture doc (`docs/architecture.md`)
- Updated LocalStack comparison report (`docs/localstack_vs_vorpalstacks_report.md`)

## [0.0.5] - 2026-04-01

### Added
- Neptune graph database service (property graph + RDF, openCypher/Gremlin, bulk loader)
- Neptune Data API (Neptunedata) with routing and request handling

### Changed
- Updated gRPC dependency from v1.79.1 to v1.79.3
- Service count updated to 30, Go SDK tests to 890 passing

### Documentation
- Added Japanese and Chinese READMEs
- Added performance benchmark section
- Added Terraform conformance tests and LocalStack comparison links
- Updated roadmap: Neptune promoted to implemented

## [0.0.4] - 2026-03-30

### Added
- Step Functions JSONata query language support (full AWS specification compliance)
- Workflow variables with scope management (Assign field on all state types)
- Built-in variables: `$states.input`, `$states.result`, `$states.context`, `$states.errorOutput`
- JSONata Output, Arguments, Items, Condition, Seconds expressions
- TestState Variables support and VariableReferences on DescribeStateMachine
- EvaluationFailed history events for JSONata query errors
- JSONata-only / JSONPath-only field validation on Create/UpdateStateMachine
- Custom AWS JSONata functions: `$uuid`, `$hash`, `$random`, `$parse`, `$partition`, `$range`
- Context object (`$states.context`) matching `$$.` intrinsic function structure
- HeartbeatSeconds / TimeoutSeconds expression evaluation in JSONata mode
- MapItemValue / MapItemIndex for Map state ItemSelector
- Catch.Assign and Catch.Output for JSONata error handling
- Lambda Function URL server, S3 website hosting, CloudFront distribution server
- Cognito hosted UI server, listener manager, gRPC-Web admin_auth registration
- Lambda and new service guide documentation

### Fixed
- InputOutput unmarshal for string values in JSONata definitions
- Choice Condition unwrapping for JSONata expressions
- Variable names incorrectly stored with `$` prefix
- VariableReferences regex, builtin filtering, and describe-time recomputation
- Double-wrapping of `states` key in variable map construction
- BuiltinFunction value leaking into JSONata expression results
- Various service log/request refactors

## [0.0.3] - 2026-03-29

### Added
- Route53 path-based routing (create/get/delete by zone name)
- Route53 NS and SOA record auto-creation on hosted zone creation
- WAF ListRules and ListRateBasedRules APIs
- XML namespace encoder for REST-XML protocols
- Integration test scripts for API Gateway → Lambda → DynamoDB and S3

### Fixed
- CloudWatch millisecond timestamps converted to epoch seconds in JSON responses
- Kinesis SubscribeToShard initial-response event and nested JSON parameter parsing
- S3 URL-decoded copy-source header
- Route53/CloudFront XML list encoding (nil-slice → empty elements)
- SSM DescribeParameters filter by type/path
- CloudFront response headers policy, invalidation, origin access control, and cache policy list operations
- Go SDK tests 594/594, Python 605/605, TypeScript 603/603, C# 622/622 — all passing

### Documentation
- Updated API coverage stats (SecretsManager 100%, StepFunctions 100%, KMS 90%)

## [0.0.2] - 2026-03-28

### Added
- IAM Service Last Accessed Details with CloudTrail integration
- IAM policy condition support (StringEquals, StringNotEquals, Bool, ArnEquals, ArnNotEquals, IpAddress, NotIpAddress, DateLessThan, DateGreaterThan, Null)
- IAM SimulatePrincipalPolicy API
- IAM EvaluateTrustPolicy with Federated principal matching
- KMS authoriseOperation() wired into all KMS handlers (~30 call sites)
- STS EvaluateTrustPolicy wired into AssumeRole, AssumeRoleWithSAML, AssumeRoleWithWebIdentity
- Secrets Manager replication APIs (ReplicateSecretToRegions, UpdateReplicationInfo, RemoveRegionsFromReplication)
- Step Functions RedriveExecution and TestState APIs
- CloudFront ListDistributionsByWebACLId API
- WAFv2 GetWebACLForResource API
- CloudFront-WAF association synchronisation
- BootstrapConfig for centralised environment variable loading

### Changed
- Replaced 14 multi-constructor variants with setter injection across 7 services (SNS, EventBridge, Step Functions, Scheduler, CloudWatch Logs, Lambda, API Gateway Runtime)
- Extracted gRPC-Web admin handler registration to `grpcweb.RegisterAllAdminHandlers()`
- Moved Cognito JWKS handler from main.go closure into `CognitoService.JWKSHandler()`
- Split large files for SRP: `s3/handler.go` → 4 files, `dynamodb/convert.go` → 4 files, `kinesis/store.go` → 5 files
- Replaced service operation counts with Full/Broad/Selective coverage tiers in documentation
- main.go reduced from 502 to 345 lines (31% reduction)

### Fixed
- IAM trust policy parsing for single-string StringList values
- IAM SLA nil dereference, XML key names, audit principal resolution
- API Gateway Runtime setter injection order (both stores now mutate same factory)
- WAF and WAFv2 now use regional storage instead of global

### Documentation
- Added LocalStack comparison report
- Refreshed services.md with coverage tiers

## [0.0.1] - 2026-03-27

### Added
- Initial public beta release
- 30 AWS services implemented (S3, SQS, SNS, Lambda, DynamoDB, IAM, KMS, Kinesis, API Gateway, Step Functions, EventBridge, CloudFront, WAF, WAFv2, Cognito, STS, Route53, CloudWatch, CloudTrail, Secrets Manager, SSM, Scheduler, SESv2, Athena, Timestream, ACM)
- 594 SDK integration tests (100% pass rate)
- gRPC-Web admin console (Flutter)
- Multi-region support with PebbleDB storage
- IAM policy evaluation
- Docker-based Lambda execution
- Terraform and OpenTofu compatibility guide
- GitHub Actions CI

### Documentation
- Architecture overview
- Service reference with operation counts
- Configuration reference
- Terraform & OpenTofu guide with 18 verified services
- Contributing guidelines
